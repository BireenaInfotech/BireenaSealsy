<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Creation Panel - Superadmin</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95);
            padding: 15px 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        .navbar-brand {
            font-size: 24px;
            font-weight: bold;
            color: #667eea;
        }

        .navbar-user {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .badge {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 600;
        }

        .btn-logout {
            background: #dc3545;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s;
        }

        .btn-logout:hover {
            background: #c82333;
            transform: translateY(-2px);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .panel {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            margin-bottom: 30px;
            animation: slideIn 0.5s ease-out;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .panel-header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f0f0f0;
        }

        h1 {
            color: #333;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #666;
            font-size: 16px;
        }

        .alert {
            padding: 15px 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            font-size: 15px;
            animation: slideIn 0.3s ease-out;
        }

        .alert-danger {
            background: #fee;
            color: #c33;
            border-left: 4px solid #c33;
        }

        .alert-success {
            background: #efe;
            color: #3c3;
            border-left: 4px solid #3c3;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
            }
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: 600;
            font-size: 14px;
        }

        .required {
            color: #dc3545;
            margin-left: 3px;
        }

        .input-wrapper {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #999;
            font-size: 18px;
        }

        input, textarea {
            width: 100%;
            padding: 15px 15px 15px 45px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 15px;
            transition: all 0.3s;
            outline: none;
            font-family: inherit;
        }

        textarea {
            padding: 15px;
            resize: vertical;
            min-height: 100px;
        }

        input:focus, textarea:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .help-text {
            font-size: 12px;
            color: #999;
            margin-top: 5px;
        }

        .btn {
            padding: 15px 35px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-right: 10px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            background: #5a6268;
        }

        .btn-container {
            text-align: center;
            margin-top: 40px;
            padding-top: 30px;
            border-top: 2px solid #f0f0f0;
        }

        .info-box {
            background: #e7f3ff;
            border-left: 4px solid #2196F3;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }

        .info-box h3 {
            color: #1976D2;
            margin-bottom: 10px;
            font-size: 18px;
        }

        .info-box ul {
            margin-left: 20px;
            color: #555;
        }

        .info-box li {
            margin-bottom: 8px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="navbar-brand">
            🏪 Multi-Shop Admin Panel
        </div>
        <div class="navbar-user">
            <span class="badge">👑 SUPERADMIN</span>
            <a href="/.hidden/logout" class="btn-logout">Logout</a>
        </div>
    </div>

    <div class="container">
        <div class="panel">
            <div class="panel-header">
                <h1>➕ Create New Shop Admin</h1>
                <p class="subtitle">Add admin credentials for a new bakery shop</p>
            </div>

            <% if (locals.error_msg && error_msg.length > 0) { %>
                <div class="alert alert-danger">
                    <strong>❌ Error:</strong> <%= error_msg[0] %>
                </div>
            <% } %>

            <% if (locals.success_msg && success_msg.length > 0) { %>
                <div class="alert alert-success">
                    <strong>✅ Success:</strong> <%= success_msg[0] %>
                </div>
            <% } %>

            <div class="info-box">
                <h3>ℹ️ Important Information</h3>
                <ul>
                    <li>Each admin manages their own shop independently</li>
                    <li>Admins can create employees for their shop</li>
                    <li>Username must be unique across all shops</li>
                    <li>Password should be strong (minimum 6 characters recommended)</li>
                </ul>
            </div>

            <form action="/.hidden/admin-panel" method="POST">
                <div class="form-row">
                    <div class="form-group">
                        <label for="username">
                            Username <span class="required">*</span>
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">👤</span>
                            <input 
                                type="text" 
                                id="username" 
                                name="username" 
                                placeholder="e.g., delhiadmin"
                                required
                                minlength="3"
                            >
                        </div>
                        <div class="help-text">Lowercase, no spaces (auto-converted)</div>
                    </div>

                    <div class="form-group">
                        <label for="password">
                            Password <span class="required">*</span>
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">🔑</span>
                            <input 
                                type="password" 
                                id="password" 
                                name="password" 
                                placeholder="Strong password"
                                required
                                minlength="6"
                            >
                        </div>
                        <div class="help-text">Minimum 6 characters</div>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="fullName">
                            Full Name
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">📝</span>
                            <input 
                                type="text" 
                                id="fullName" 
                                name="fullName" 
                                placeholder="e.g., Rajesh Kumar"
                            >
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="phone">
                            Phone Number
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">📱</span>
                            <input 
                                type="tel" 
                                id="phone" 
                                name="phone" 
                                placeholder="10-digit number"
                                pattern="[0-9]{10}"
                                maxlength="10"
                            >
                        </div>
                        <div class="help-text">Must be 10 digits</div>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="email">
                            Email Address
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">📧</span>
                            <input 
                                type="email" 
                                id="email" 
                                name="email" 
                                placeholder="admin@shop.com"
                            >
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="shopName">
                            Shop Name
                        </label>
                        <div class="input-wrapper">
                            <span class="input-icon">🏪</span>
                            <input 
                                type="text" 
                                id="shopName" 
                                name="shopName" 
                                placeholder="e.g., Bireena Bakery Delhi"
                            >
                        </div>
                    </div>
                </div>

                <div class="form-group full-width">
                    <label for="shopAddress">
                        Shop Address
                    </label>
                    <textarea 
                        id="shopAddress" 
                        name="shopAddress" 
                        placeholder="Complete shop address with city and pincode"
                    ></textarea>
                </div>

                <div class="form-group full-width">
                    <label for="shopGST">
                        GST Number (Optional)
                    </label>
                    <div class="input-wrapper">
                        <span class="input-icon">📄</span>
                        <input 
                            type="text" 
                            id="shopGST" 
                            name="shopGST" 
                            placeholder="e.g., 27AABCU9603R1ZM"
                        >
                    </div>
                </div>

                <div class="btn-container">
                    <button type="submit" class="btn btn-primary">
                        ✨ Create Admin Account
                    </button>
                    <button type="reset" class="btn btn-secondary">
                        🔄 Reset Form
                    </button>
                </div>
            </form>
        </div>

        <!-- Shop Management Section -->
        <div class="panel" style="margin-top: 30px;">
            <div class="panel-header">
                <h1>🏪 Manage Shops</h1>
                <p class="subtitle">View and manage all registered shops</p>
            </div>

            <% if (locals.admins && admins.length > 0) { %>
                <div style="background: #f8f9fa; padding: 15px; border-radius: 10px; margin-bottom: 20px;">
                    <strong>📊 Total Shops:</strong> <%= admins.length %>
                </div>

                <div style="overflow-x: auto;">
                    <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
                        <thead>
                            <tr style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;">
                                <th style="padding: 15px; text-align: left; border-radius: 10px 0 0 0;">#</th>
                                <th style="padding: 15px; text-align: left;">Shop Details</th>
                                <th style="padding: 15px; text-align: left;">Username</th>
                                <th style="padding: 15px; text-align: center;">Status</th>
                                <th style="padding: 15px; text-align: center;">Pack Expiry</th>
                                <th style="padding: 15px; text-align: center; border-radius: 0 10px 0 0;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% admins.forEach((admin, index) => { %>
                                <% 
                                    const isExpired = admin.packExpiry && new Date(admin.packExpiry) < new Date();
                                    const daysLeft = admin.packExpiry ? Math.ceil((new Date(admin.packExpiry) - new Date()) / (1000 * 60 * 60 * 24)) : null;
                                %>
                                <tr style="border-bottom: 1px solid #eee; <%= !admin.isActive || isExpired ? 'background: #fff5f5;' : '' %>">
                                    <td style="padding: 15px;"><%= index + 1 %></td>
                                    <td style="padding: 15px;">
                                        <div style="font-weight: 600; color: #333;"><%= admin.fullName || 'N/A' %></div>
                                        <div style="font-size: 13px; color: #666;"><%= admin.shopName || 'No shop name' %></div>
                                        <div style="font-size: 12px; color: #999;">Branch: <%= admin.branch %></div>
                                    </td>
                                    <td style="padding: 15px;">
                                        <code style="background: #f0f0f0; padding: 5px 10px; border-radius: 5px;"><%= admin.username %></code>
                                    </td>
                                    <td style="padding: 15px; text-align: center;">
                                        <% if (!admin.isActive) { %>
                                            <span style="background: #dc3545; color: white; padding: 5px 15px; border-radius: 20px; font-size: 13px; font-weight: 600;">
                                                🚫 SUSPENDED
                                            </span>
                                            <% if (admin.suspendedReason) { %>
                                                <div style="font-size: 11px; color: #999; margin-top: 5px;"><%= admin.suspendedReason %></div>
                                            <% } %>
                                        <% } else if (isExpired) { %>
                                            <span style="background: #ff9800; color: white; padding: 5px 15px; border-radius: 20px; font-size: 13px; font-weight: 600;">
                                                ⏰ EXPIRED
                                            </span>
                                        <% } else { %>
                                            <span style="background: #28a745; color: white; padding: 5px 15px; border-radius: 20px; font-size: 13px; font-weight: 600;">
                                                ✅ ACTIVE
                                            </span>
                                        <% } %>
                                    </td>
                                    <td style="padding: 15px; text-align: center;">
                                        <% if (admin.packExpiry) { %>
                                            <div style="font-size: 13px; <%= isExpired ? 'color: #dc3545; font-weight: 600;' : daysLeft <= 7 ? 'color: #ff9800; font-weight: 600;' : 'color: #666;' %>">
                                                <%= new Date(admin.packExpiry).toLocaleDateString('en-GB') %>
                                            </div>
                                            <% if (!isExpired && daysLeft !== null) { %>
                                                <div style="font-size: 11px; color: #999;"><%= daysLeft %> days left</div>
                                            <% } %>
                                            <form action="/.hidden/update-expiry/<%= admin._id %>" method="POST" style="margin-top: 5px;">
                                                <input type="date" name="expiryDate" value="<%= new Date(admin.packExpiry).toISOString().split('T')[0] %>" style="padding: 3px; font-size: 11px; border: 1px solid #ddd; border-radius: 4px;">
                                                <button type="submit" style="padding: 3px 8px; font-size: 11px; background: #667eea; color: white; border: none; border-radius: 4px; cursor: pointer; margin-left: 3px;">✓</button>
                                            </form>
                                        <% } else { %>
                                            <div style="font-size: 13px; color: #28a745; font-weight: 600;">∞ Unlimited</div>
                                            <form action="/.hidden/update-expiry/<%= admin._id %>" method="POST" style="margin-top: 5px;">
                                                <input type="date" name="expiryDate" style="padding: 3px; font-size: 11px; border: 1px solid #ddd; border-radius: 4px;">
                                                <button type="submit" style="padding: 3px 8px; font-size: 11px; background: #667eea; color: white; border: none; border-radius: 4px; cursor: pointer; margin-left: 3px;">Set</button>
                                            </form>
                                        <% } %>
                                    </td>
                                    <td style="padding: 15px; text-align: center;">
                                        <div style="display: flex; gap: 5px; justify-content: center; flex-wrap: wrap;">
                                            <form action="/.hidden/toggle-shop/<%= admin._id %>" method="POST" style="display: inline;">
                                                <% if (admin.isActive) { %>
                                                    <input type="hidden" name="reason" value="Suspended by superadmin">
                                                    <button type="submit" onclick="return confirm('Suspend this shop? They won\'t be able to login.')" style="padding: 8px 15px; background: #ffc107; color: #333; border: none; border-radius: 6px; cursor: pointer; font-size: 12px; font-weight: 600;">
                                                        🚫 Suspend
                                                    </button>
                                                <% } else { %>
                                                    <button type="submit" style="padding: 8px 15px; background: #28a745; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 12px; font-weight: 600;">
                                                        ✅ Activate
                                                    </button>
                                                <% } %>
                                            </form>
                                            <form action="/.hidden/delete-shop/<%= admin._id %>" method="POST" style="display: inline;">
                                                <button type="submit" onclick="return confirm('⚠️ DELETE <%= admin.username %>? This will remove ALL data (products, sales, employees). This action CANNOT be undone!')" style="padding: 8px 15px; background: #dc3545; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 12px; font-weight: 600;">
                                                    🗑️ Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div style="text-align: center; padding: 40px; color: #999;">
                    <div style="font-size: 48px; margin-bottom: 20px;">🏪</div>
                    <h3>No shops registered yet</h3>
                    <p>Create your first shop admin using the form above</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        // Auto-lowercase username
        document.getElementById('username').addEventListener('input', function(e) {
            e.target.value = e.target.value.toLowerCase().replace(/\s/g, '');
        });

        // Show success message for 5 seconds
        const successAlert = document.querySelector('.alert-success');
        if (successAlert) {
            setTimeout(() => {
                successAlert.style.transition = 'opacity 0.5s';
                successAlert.style.opacity = '0';
                setTimeout(() => successAlert.remove(), 500);
            }, 5000);
        }
    </script>
</body>
</html>
