<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
    <title>Welcome to Bireena Salesy</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
        :root {
            --primary-purple: #7c3aed;
            --primary-purple-dark: #6d28d9;
            --secondary-pink: #ec4899;
            --accent-cyan: #06b6d4;
            --accent-cyan-light: #22d3ee;
            --dark-bg: #0f0318;
            --dark-bg-alt: #1a0b2e;
            --text-primary: #ffffff;
            --text-secondary: #cbd5e1;
            --text-muted: #94a3b8;
            --font-heading: 'Playfair Display', serif;
            --font-body: 'Poppins', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            overflow-x: hidden;
            width: 100%;
        }

        body {
            font-family: var(--font-body);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
            width: 100%;
            max-width: 100vw;
        }

        .home-page {
            position: relative;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            width: 100%;
            max-width: 100vw;
            overflow-x: hidden;
        }

        .hero-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: 
                radial-gradient(circle at 20% 50%, rgba(124, 58, 237, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(236, 72, 153, 0.3) 0%, transparent 50%),
                linear-gradient(135deg, #0f0318 0%, #1a0b2e 50%, #2d1b4e 100%);
            background-attachment: fixed;
        }

        .hero-background::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(124, 58, 237, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(124, 58, 237, 0.05) 1px, transparent 1px);
            background-size: 50px 50px;
            opacity: 0.3;
        }

        .hero-background::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(2px 2px at 20% 30%, rgba(255, 255, 255, 0.3), transparent),
                            radial-gradient(2px 2px at 60% 70%, rgba(34, 211, 238, 0.4), transparent),
                            radial-gradient(1px 1px at 50% 50%, rgba(236, 72, 153, 0.3), transparent);
            background-size: 200px 200px, 300px 300px, 150px 150px;
            animation: particleFloat 20s ease-in-out infinite;
        }

        @keyframes particleFloat {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(30px, -30px); }
        }

        /* ===== RESPONSIVE HEADER & NAVIGATION ===== */
        :root {
            --header-height: 75px;
        }

        .top-navigation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 0 2rem;
            z-index: 1000;
            background: rgba(15, 3, 24, 0.98);
            backdrop-filter: blur(15px);
            border-bottom: 1px solid rgba(124, 58, 237, 0.15);
            height: var(--header-height);
            display: flex;
            align-items: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }

        .nav-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1400px;
            margin: 0 auto;
            width: 100%;
            height: 100%;
        }

        .site-title {
            font-size: 1.4rem;
            font-weight: 700;
            letter-spacing: 0.3px;
            color: var(--text-primary);
            font-family: var(--font-heading);
            cursor: pointer;
            z-index: 1001;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.25rem;
        }

        .site-title .gradient-text {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Hamburger Menu Button */
        .hamburger-menu {
            display: none;
            flex-direction: column;
            gap: 5px;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            z-index: 1001;
            transition: transform 0.2s ease;
            touch-action: manipulation; /* Optimize touch response */
            -webkit-tap-highlight-color: transparent; /* Remove tap highlight */
        }

        .hamburger-menu:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
            border-radius: 4px;
        }

        .hamburger-line {
            width: 25px;
            height: 3px;
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            border-radius: 3px;
            transition: all 0.2s ease;
            pointer-events: none; /* Prevent touch delay */
        }

        .hamburger-menu.active .hamburger-line:nth-child(1) {
            transform: translateY(8px) rotate(45deg);
        }

        .hamburger-menu.active .hamburger-line:nth-child(2) {
            opacity: 0;
        }

        .hamburger-menu.active .hamburger-line:nth-child(3) {
            transform: translateY(-8px) rotate(-45deg);
        }

        /* Navigation Menu */
        .nav-menu {
            display: flex;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link:hover, .nav-link.active {
            color: var(--text-primary);
        }

        .nav-link:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
            border-radius: 4px;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--accent-cyan), var(--primary-purple));
            transition: width 0.3s ease;
        }

        .nav-link:hover::after, .nav-link.active::after {
            width: 100%;
        }

        .nav-login-btn {
            padding: 0.65rem 2rem;
            background: linear-gradient(135deg, #8b5cf6 0%, #06b6d4 100%);
            color: #ffffff;
            border: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.95rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
            white-space: nowrap;
        }

        .nav-login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 25px rgba(139, 92, 246, 0.5);
        }

        .nav-login-btn:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
        }

        .hero-section {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 4rem 2rem;
            position: relative;
            margin-top: var(--header-height);
            scroll-margin-top: var(--header-height);
        }

        .hero-container {
            max-width: 1400px;
            width: 100%;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            padding: 0 1rem;
        }

        .hero-content {
            animation: fadeInUp 1s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero-top-text {
            font-size: 0.875rem;
            font-weight: 600;
            letter-spacing: 2px;
            color: var(--accent-cyan);
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            word-wrap: break-word;
            overflow-wrap: break-word;
            max-width: 100%;
        }

        .welcome-title {
            font-family: var(--font-heading);
            font-size: 3.75rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #ffffff 0%, var(--accent-cyan) 50%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.2;
            text-shadow: 0 0 40px rgba(124, 58, 237, 0.3);
            word-wrap: break-word;
            overflow-wrap: break-word;
            hyphens: auto;
            max-width: 100%;
        }

        .gradient-highlight {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 3s ease-in-out infinite;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .welcome-subtitle {
            font-size: 1.125rem;
            color: var(--text-secondary);
            margin-bottom: 2.5rem;
            font-weight: 400;
            line-height: 1.8;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .hero-illustration {
            display: flex;
            align-items: center;
            justify-content: center;
            animation: fadeInUp 1.2s ease-out;
        }

        .hero-illustration img {
            width: 100%;
            max-width: 550px;
            height: auto;
            filter: drop-shadow(0 20px 40px rgba(124, 58, 237, 0.3));
            animation: floatAnimation 6s ease-in-out infinite;
            object-fit: contain;
        }

        /* Global Image and Container Overflow Fix */
        img, svg, video {
            max-width: 100%;
            height: auto;
        }

        section, div {
            max-width: 100%;
        }

        @keyframes floatAnimation {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        .btn {
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            color: #ffffff;
            border: none;
            box-shadow: 0 4px 20px rgba(124, 58, 237, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 30px rgba(124, 58, 237, 0.6);
        }

        .btn-secondary {
            background: rgba(30, 15, 50, 0.7);
            backdrop-filter: blur(20px);
            color: #ffffff;
            border: 2px solid rgba(124, 58, 237, 0.5);
            box-shadow: 0 4px 20px rgba(124, 58, 237, 0.3);
        }

        .btn-secondary:hover {
            background: rgba(40, 25, 60, 0.8);
            border-color: rgba(168, 85, 247, 0.7);
            transform: translateY(-3px);
            box-shadow: 0 6px 30px rgba(124, 58, 237, 0.5);
        }

        /* Why Choose Section */
        .why-choose-section {
            position: relative;
            padding: 4rem 2rem;
            max-width: 1400px;
            margin: 0 auto;
            width: 100%;
            overflow-x: hidden;
        }

        .section-header {
            text-align: center;
            margin-bottom: 4rem;
            width: 100%;
            padding: 0 1rem;
        }

        .section-title {
            font-family: var(--font-heading);
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--text-primary);
            word-wrap: break-word;
        }

        .section-title .gradient-highlight {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-subtitle {
            font-size: 1.125rem;
            color: var(--text-secondary);
            max-width: 900px;
            margin: 0 auto;
            line-height: 1.8;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            margin-top: 3rem;
        }

        .feature-card {
            position: relative;
            padding: 2rem;
            border-radius: 20px;
            background: rgba(30, 15, 50, 0.6);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(124, 58, 237, 0.3);
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 20px;
            padding: 1px;
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.5), rgba(124, 58, 237, 0.5));
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(124, 58, 237, 0.4);
            border-color: rgba(124, 58, 237, 0.6);
        }

        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-card-content {
            position: relative;
            display: flex;
            gap: 1.25rem;
            align-items: flex-start;
        }

        .feature-icon {
            flex-shrink: 0;
            width: 60px;
            height: 60px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.75rem;
            transition: all 0.3s ease;
        }

        .feature-card:nth-child(1) .feature-icon {
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(6, 182, 212, 0.1));
            color: var(--accent-cyan);
        }

        .feature-card:nth-child(2) .feature-icon {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), rgba(139, 92, 246, 0.1));
            color: #8b5cf6;
        }

        .feature-card:nth-child(3) .feature-icon {
            background: linear-gradient(135deg, rgba(236, 72, 153, 0.2), rgba(236, 72, 153, 0.1));
            color: var(--secondary-pink);
        }

        .feature-card:nth-child(4) .feature-icon {
            background: linear-gradient(135deg, rgba(34, 211, 238, 0.2), rgba(34, 211, 238, 0.1));
            color: var(--accent-cyan-light);
        }

        .feature-card:nth-child(5) .feature-icon {
            background: linear-gradient(135deg, rgba(124, 58, 237, 0.2), rgba(124, 58, 237, 0.1));
            color: var(--primary-purple);
        }

        .feature-card:nth-child(6) .feature-icon {
            background: linear-gradient(135deg, rgba(168, 85, 247, 0.2), rgba(168, 85, 247, 0.1));
            color: #a855f7;
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1);
        }

        .feature-text {
            flex: 1;
        }

        .feature-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .feature-description {
            font-size: 0.95rem;
            color: var(--text-muted);
            line-height: 1.6;
        }

        /* Meet Our Owner Section */
        .owner-section {
            position: relative;
            padding: 4rem 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .owner-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2.5rem;
            align-items: center;
        }

        .owner-image-wrapper {
            position: relative;
            animation: fadeInUp 1s ease-out;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            width: 400px;
            height: 400px;
            border-radius: 50%;
            transition: transform 0.4s ease;
        }

        .owner-image-wrapper:hover {
            transform: scale(1.05);
        }

        .owner-image-wrapper::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            max-width: 400px;
            max-height: 400px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            opacity: 0;
            transition: opacity 0.4s ease;
            z-index: -1;
            filter: blur(20px);
        }

        .owner-image-wrapper:hover::before {
            opacity: 0.6;
        }

        .owner-image {
            width: 400px;
            height: 400px;
            object-fit: cover;
            object-position: center center;
            border-radius: 50%;
            border: 4px solid rgba(124, 58, 237, 0.4);
            box-shadow: 0 20px 60px rgba(124, 58, 237, 0.4);
            transition: all 0.4s ease;
            cursor: pointer;
            display: block;
            transform: scale(1.15);
        }

        .owner-image:hover {
            transform: scale(1.22);
            box-shadow: 0 30px 80px rgba(6, 182, 212, 0.8), 0 0 40px rgba(124, 58, 237, 0.6);
            border-color: var(--accent-cyan);
            filter: brightness(1.1);
        }

        .owner-content {
            animation: fadeInUp 1.2s ease-out;
            max-width: 100%;
            overflow-wrap: break-word;
            word-wrap: break-word;
        }

        .owner-heading {
            font-family: var(--font-heading);
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: var(--text-primary);
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        .owner-subheading {
            font-size: 1.125rem;
            color: var(--accent-cyan);
            margin-bottom: 2rem;
            font-weight: 500;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        .owner-text {
            font-size: 1rem;
            color: var(--text-secondary);
            line-height: 1.8;
            margin-bottom: 1.5rem;
            word-wrap: break-word;
            overflow-wrap: break-word;
            hyphens: auto;
        }

        .owner-quote {
            font-size: 1.125rem;
            font-style: italic;
            word-wrap: break-word;
            overflow-wrap: break-word;
            hyphens: auto;
            color: var(--text-primary);
            margin: 2rem 0;
            padding-left: 1.5rem;
            border-left: 3px solid var(--accent-cyan);
            opacity: 0.9;
        }

        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 2.5rem;
            flex-wrap: wrap;
        }

        .social-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            background: rgba(30, 15, 50, 0.6);
            backdrop-filter: blur(20px);
            border: 2px solid rgba(124, 58, 237, 0.4);
            color: var(--text-primary);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .social-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(6, 182, 212, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .social-btn:hover::before {
            left: 100%;
        }

        .social-btn:hover {
            transform: translateY(-3px);
            border-color: var(--accent-cyan);
            box-shadow: 0 8px 25px rgba(6, 182, 212, 0.4);
        }

        .social-btn.instagram:hover {
            border-color: #E1306C;
            box-shadow: 0 8px 25px rgba(225, 48, 108, 0.4);
        }

        .social-btn.facebook:hover {
            border-color: #1877F2;
            box-shadow: 0 8px 25px rgba(24, 119, 242, 0.4);
        }

        .social-btn.youtube:hover {
            border-color: #FF0000;
            box-shadow: 0 8px 25px rgba(255, 0, 0, 0.4);
        }

        .social-icon {
            font-size: 1.25rem;
        }

        .dot:hover {
            background: var(--primary-purple);
        }

        /* Contact Section */
        .contact-section {
            position: relative;
            padding: 4rem 2rem 5rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .contact-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .contact-title {
            font-family: var(--font-heading);
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #ffffff 0%, var(--accent-cyan) 50%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .contact-subtitle {
            font-size: 1.125rem;
            color: var(--text-secondary);
            max-width: 700px;
            margin: 0 auto;
            line-height: 1.8;
        }

        .contact-card {
            max-width: 700px;
            margin: 0 auto;
            padding: 3rem;
            background: rgba(30, 15, 50, 0.6);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(124, 58, 237, 0.3);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(124, 58, 237, 0.3);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-size: 0.95rem;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .form-input,
        .form-textarea {
            width: 100%;
            padding: 0.875rem 1rem;
            background: rgba(15, 5, 30, 0.6);
            border: 1px solid rgba(124, 58, 237, 0.3);
            border-radius: 12px;
            color: var(--text-primary);
            font-size: 1rem;
            font-family: var(--font-body);
            transition: all 0.3s ease;
        }

        .form-input::placeholder,
        .form-textarea::placeholder {
            color: var(--text-muted);
        }

        .form-input:focus,
        .form-textarea:focus {
            outline: none;
            border-color: var(--accent-cyan);
            box-shadow: 0 0 0 3px rgba(6, 182, 212, 0.1);
        }

        .form-textarea {
            resize: vertical;
            min-height: 120px;
        }

        .submit-btn {
            width: 100%;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            color: #ffffff;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 20px rgba(124, 58, 237, 0.4);
        }

        .submit-btn:hover:not(:disabled) {
            transform: translateY(-3px);
            box-shadow: 0 6px 30px rgba(124, 58, 237, 0.6);
        }

        .submit-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .form-message {
            margin-top: 1rem;
            padding: 1rem;
            border-radius: 12px;
            text-align: center;
            font-size: 0.95rem;
            display: none;
        }

        .form-message.success {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            color: #4ade80;
            display: block;
        }

        .form-message.error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #f87171;
            display: block;
        }

        /* Footer Section */
        .footer-section {
            position: relative;
            background: linear-gradient(135deg, #0a0515 0%, #0d0820 50%, #1a0b2e 100%);
            border-top: 1px solid rgba(124, 58, 237, 0.3);
            padding: 4rem 2rem 0;
        }

        .footer-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1.5fr;
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-column {
            display: flex;
            flex-direction: column;
        }

        /* Footer Card Styling */
        .footer-card {
            background: rgba(15, 6, 40, 0.6);
            border: 1px solid rgba(124, 58, 237, 0.3);
            border-radius: 16px;
            padding: 2rem;
            backdrop-filter: blur(10px);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            height: 100%;
        }

        .footer-card:hover {
            background: rgba(20, 10, 50, 0.8);
            border-color: rgba(6, 182, 212, 0.6);
            box-shadow: 0 8px 32px rgba(6, 182, 212, 0.2),
                        0 0 40px rgba(124, 58, 237, 0.15);
            transform: translateY(-5px);
        }

        .footer-brand {
            font-family: var(--font-heading);
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            letter-spacing: 0.5px;
        }

        .footer-brand .gradient-text {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .footer-info {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .footer-info-item {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .footer-icon {
            color: var(--accent-cyan);
            font-size: 1.25rem;
            flex-shrink: 0;
            margin-top: 0.125rem;
        }

        .footer-social {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .social-icon-link {
            width: 42px;
            height: 42px;
            border-radius: 50%;
            background: rgba(124, 58, 237, 0.15);
            border: 1px solid rgba(124, 58, 237, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent-cyan);
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .social-icon-link:hover {
            background: rgba(6, 182, 212, 0.2);
            border-color: var(--accent-cyan);
            box-shadow: 0 0 20px rgba(6, 182, 212, 0.4);
            transform: translateY(-3px) scale(1.05);
        }

        .footer-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            font-family: var(--font-heading);
            letter-spacing: 0.3px;
        }

        .footer-links {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .footer-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
            padding-left: 1.25rem;
        }

        .footer-link::before {
            content: '→';
            color: var(--accent-cyan);
            position: absolute;
            left: 0;
            opacity: 0;
            transform: translateX(-5px);
            transition: all 0.3s ease;
        }

        .footer-link:hover {
            color: var(--accent-cyan);
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.3);
            padding-left: 1.5rem;
        }

        .footer-link:hover::before {
            opacity: 1;
            transform: translateX(0);
        }

        .footer-text {
            color: var(--text-muted);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* Hours Content Inside Card */
        .hours-content {
            display: flex;
            flex-direction: column;
            gap: 0;
        }

        .hours-item {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(124, 58, 237, 0.15);
        }

        .hours-item:first-child {
            padding-top: 0;
        }

        .hours-item:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .hours-day {
            color: var(--text-secondary);
            font-size: 0.95rem;
            font-weight: 400;
        }

        .hours-time {
            color: var(--accent-cyan);
            font-weight: 600;
            font-size: 1rem;
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.3);
        }

        .footer-bottom {
            border-top: 1px solid rgba(124, 58, 237, 0.2);
            padding: 2rem 0;
            text-align: center;
        }

        .copyright {
            color: var(--text-muted);
            font-size: 0.875rem;
        }

        .copyright a {
            color: var(--accent-cyan);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .copyright a:hover {
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.8);
        }



        /* Section scroll offset for fixed header */
        section {
            scroll-margin-top: var(--header-height);
        }

        /* ===== RESPONSIVE BREAKPOINTS ===== */

        /* Tablet (1024px and below) */
        @media (max-width: 1024px) {
            :root {
                --header-height: 70px;
            }

            .top-navigation {
                padding: 1rem 2rem;
            }

            .hamburger-menu {
                display: flex;
                order: 3;
            }

            .site-title {
                order: 1;
                font-size: 1.3rem;
            }

            .nav-menu {
                position: fixed;
                top: var(--header-height);
                left: 0;
                width: 100%;
                max-width: 400px;
                height: calc(100vh - var(--header-height));
                background: rgba(15, 3, 24, 0.98);
                backdrop-filter: blur(20px);
                flex-direction: column;
                justify-content: flex-start;
                padding: 2rem;
                gap: 0;25s cubic-bezier(0.4, 0, 0.2, 1); /* Faster smooth animation */
                border-right: 1px solid rgba(124, 58, 237, 0.3);
                overflow-y: auto;
                box-shadow: 4px 0 20px rgba(0, 0, 0, 0.5);
                will-change: transform; /* Hardware acceleration */ 0.3);
                overflow-y: auto;
                box-shadow: 4px 0 20px rgba(0, 0, 0, 0.5);
            }

            .nav-menu.active {
                transform: translateX(0);
            }

            .nav-link {
                width: 100%;
                padding: 1rem;
                font-size: 1.1rem;
                border-bottom: 1px solid rgba(124, 58, 237, 0.2);
                text-align: left;
            }

            .nav-link::after {
                display: none;
            }

            .nav-login-btn {
                display: none !important;
            }

            .nav-link-mobile-login {
                display: block !important;
            }

            .nav-login-btn {
                order: 2;
                padding: 0.5rem 1.5rem;
                font-size: 0.85rem;
            }

            /* Mobile menu variant - include login in menu */
            .nav-menu .nav-login-mobile {
                display: flex !important;
                align-items: center;
                justify-content: center;
                gap: 0.5rem;
                width: 100%;
                margin-top: 1.5rem;
                padding: 1rem 2rem;
                font-size: 1rem;
                text-align: center;
                background: linear-gradient(135deg, var(--primary-purple) 0%, var(--secondary-pink) 100%);
                border-radius: 12px;
                font-weight: 600;
                transition: all 0.3s ease;
            }
            
            .nav-menu .nav-login-mobile:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 20px rgba(124, 58, 237, 0.4);
            }
            
            .nav-menu .nav-login-mobile i {
                font-size: 1.2rem;
            }

            /* Overlay for mobile menu */
            .nav-overlay {
                position: fixed;
                top: var(--header-height);
                left: 0;
                width: 100%;
                height: calc(100vh - var(--header-height));
                background: rgba(0, 0, 0, 0.7);
                opacity: 0;
                visibility: hidden;
                transition: opacity 0.3s ease, visibility 0.3s ease;
                z-index: 999;
            }

            .nav-overlay.active {
                opacity: 1;
                visibility: visible;
            }
        }

        /* Mobile (768px and below) */
        @media (max-width: 768px) {
            :root {
                --header-height: 65px;
            }

            .top-navigation {
                padding: 1rem 1.5rem;
            }

            .site-title {
                font-size: 1.2rem;
            }

            .nav-login-btn {
                padding: 0.5rem 1.2rem;
                font-size: 0.8rem;
            }

            .nav-menu {
                max-width: 100%;
            }

            .hero-container {
                grid-template-columns: 1fr;
                gap: 3rem;
                padding-top: 2rem;
            }

            .hero-illustration {
                order: 2;
            }

            .hero-content {
                order: 1;
            }

            .hero-top-text {
                font-size: 0.75rem;
            }
            
            .welcome-title {
                font-size: 2.25rem;
            }
            
            .welcome-subtitle {
                font-size: 1rem;
            }

            .cta-buttons {
                justify-content: flex-start;
            }

            .btn {
                width: 100%;
                max-width: 300px;
            }

            .hero-illustration img {
                max-width: 400px;
            }

            .section-title {
                font-size: 2rem;
            }

            .section-subtitle {
                font-size: 1rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .owner-container {
                grid-template-columns: 1fr;
                gap: 2.5rem;
            }

            .owner-image-wrapper {
                order: 1;
                width: 300px;
                height: 300px;
                margin: 0 auto;
            }

            .owner-image {
                width: 300px;
                height: 300px;
            }

            .owner-image-wrapper::before {
                max-width: 300px;
                max-height: 300px;
            }

            .owner-content {
                order: 2;
                padding: 0 1rem;
            }

            .owner-heading {
                font-size: 2rem;
                word-wrap: break-word;
                overflow-wrap: break-word;
            }

            .owner-subheading {
                font-size: 1rem;
                word-wrap: break-word;
                overflow-wrap: break-word;
            }

            .owner-text {
                font-size: 0.95rem;
                word-wrap: break-word;
                overflow-wrap: break-word;
                hyphens: auto;
            }

            .owner-quote {
                font-size: 1rem;
                word-wrap: break-word;
                overflow-wrap: break-word;
                hyphens: auto;
            }

            .social-links {
                justify-content: flex-start;
                flex-wrap: wrap;
            }

            .social-btn {
                font-size: 0.875rem;
                padding: 0.65rem 1.25rem;
                word-wrap: break-word;
            }

            .contact-title {
                font-size: 2rem;
            }

            .contact-subtitle {
                font-size: 1rem;
            }

            .contact-card {
                padding: 2rem 1.5rem;
            }

            .footer-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .footer-section {
                padding: 3rem 1.5rem 0;
            }
        }

        /* Small Mobile (480px and below) */
        @media (max-width: 480px) {
            :root {
                --header-height: 60px;
            }

            .top-navigation {
                padding: 0.75rem 0.75rem;
            }

            .site-title {
                font-size: 0.95rem;
                word-spacing: -2px;
            }

            .nav-login-btn {
                padding: 0.4rem 0.9rem;
                font-size: 0.7rem;
            }

            .hero-section {
                padding: 2rem 0.75rem;
            }

            .hero-container {
                padding-top: 1rem;
            }

            .hero-content {
                padding: 0 0.5rem;
            }

            .welcome-title {
                font-size: 1.6rem;
                line-height: 1.15;
                word-wrap: break-word;
                overflow-wrap: break-word;
                word-break: break-word;
                hyphens: auto;
            }

            .hero-top-text {
                font-size: 0.65rem;
                letter-spacing: 1px;
                word-spacing: -1px;
            }

            .section-title {
                font-size: 1.75rem;
                word-wrap: break-word;
                overflow-wrap: break-word;
            }

            .btn {
                padding: 0.75rem 1.5rem;
                font-size: 0.9rem;
            }

            .welcome-subtitle {
                font-size: 0.9rem;
                line-height: 1.6;
                padding: 0 0.25rem;
            }

            .btn {
                font-size: 0.85rem;
                padding: 0.7rem 1.3rem;
            }

            .owner-section {
                padding: 2rem 0.5rem;
            }

            .owner-container {
                gap: 1.5rem;
                padding: 0 0.5rem;
            }

            .owner-image-wrapper,
            .owner-image {
                width: 220px;
                height: 220px;
            }

            .owner-heading {
                font-size: 1.6rem;
                line-height: 1.2;
                word-break: break-word;
            }

            .owner-subheading {
                font-size: 0.85rem;
                word-break: break-word;
            }

            .owner-text {
                font-size: 0.85rem;
                line-height: 1.65;
                word-break: break-word;
            }

            .owner-quote {
                font-size: 0.9rem;
                padding: 0.65rem 0.85rem;
                word-break: break-word;
            }

            .social-btn {
                font-size: 0.75rem;
                padding: 0.5rem 0.9rem;
                width: 100%;
                justify-content: center;
            }

            .contact-title {
                font-size: 2rem;
            }

            .contact-subtitle {
                font-size: 1rem;
            }

            .contact-card {
                padding: 2rem 1.5rem;
            }

            .footer-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .footer-section {
                padding: 3rem 1.5rem 0;
            }
        }

        @media (max-width: 1024px) and (min-width: 769px) {
            .features-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .footer-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* High DPI Screens */
        @media (min-width: 1280px) {
            .nav-container,
            .hero-container,
            .section-content {
                max-width: 1400px;
            }
        }
    </style>
</head>
<body>
    <div class="home-page">
        <div class="hero-background"></div>
        
        <!-- Navigation Overlay (for mobile) -->
        <div class="nav-overlay" id="navOverlay" aria-hidden="true"></div>
        
        <header class="top-navigation" role="banner">
            <div class="nav-container">
                <div class="site-title" onclick="window.location.href='/'" role="button" tabindex="0" aria-label="Bireena Salesy Home">
                    BIREENA<span class="gradient-text">SALESY</span>
                </div>
                
            <nav class="nav-menu" id="navMenu" role="navigation" aria-label="Main navigation">
                <a href="/" class="nav-link active" aria-current="page">Home</a>
                <a href="/about" class="nav-link">About</a>
                <a href="/features" class="nav-link">Features</a>
                <a href="/pricing" class="nav-link">Pricing</a>
                <a href="/contact" class="nav-link">Contact</a>
                <a href="/login" class="nav-link nav-link-mobile-login" style="display: none; margin-top: 1rem; background: linear-gradient(135deg, #8b5cf6 0%, #06b6d4 100%); color: white; border-radius: 12px; text-align: center; border: none;">Login →</a>
            </nav>
            
            <button class="nav-login-btn" onclick="window.location.href='/login'" aria-label="Login">Login →</button>                <button class="hamburger-menu" id="hamburgerBtn" aria-label="Toggle navigation menu" aria-expanded="false" aria-controls="navMenu">
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                </button>
            </div>
        </header>

        <section class="hero-section" id="hero">
            <div class="hero-container">
                <div class="hero-content">
                    <div class="hero-top-text">SMART SALES MANAGEMENT</div>
                    <h1 class="welcome-title">
                        Experience Powerful<br><span class="gradient-highlight">Sales Management</span>
                    </h1>
                    <p class="welcome-subtitle">
                        Experience a powerful, easy-to-use sales management platform designed for shops of any size. From daily transactions to full business analytics, everything is built for accuracy, speed, and convenience.
                    </p>
                    <div class="cta-buttons">
                        <a href="/login" class="btn btn-primary">Check Now</a>
                    </div>
                </div>
                <div class="hero-illustration">
                    <img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 500 400'%3E%3Cdefs%3E%3ClinearGradient id='grad1' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%2306b6d4;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%237c3aed;stop-opacity:1' /%3E%3C/linearGradient%3E%3ClinearGradient id='grad2' x1='0%25' y1='0%25' x2='100%25' y2='0%25'%3E%3Cstop offset='0%25' style='stop-color:%238b5cf6;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%2322d3ee;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect x='80' y='60' width='340' height='280' rx='20' fill='%231a0b2e' stroke='url(%23grad1)' stroke-width='3'/%3E%3Crect x='100' y='80' width='300' height='40' rx='8' fill='url(%23grad2)' opacity='0.3'/%3E%3Ctext x='120' y='105' font-family='Arial' font-size='16' fill='%23ffffff' font-weight='bold'%3ESales Dashboard%3C/text%3E%3Crect x='100' y='140' width='140' height='80' rx='12' fill='%232d1b4e' stroke='%2306b6d4' stroke-width='2'/%3E%3Ctext x='120' y='165' font-family='Arial' font-size='12' fill='%2394a3b8'%3ETotal Sales%3C/text%3E%3Ctext x='120' y='195' font-family='Arial' font-size='24' fill='%2322d3ee' font-weight='bold'%3E₹45,890%3C/text%3E%3Crect x='260' y='140' width='140' height='80' rx='12' fill='%232d1b4e' stroke='%237c3aed' stroke-width='2'/%3E%3Ctext x='280' y='165' font-family='Arial' font-size='12' fill='%2394a3b8'%3ETransactions%3C/text%3E%3Ctext x='300' y='195' font-family='Arial' font-size='24' fill='%238b5cf6' font-weight='bold'%3E234%3C/text%3E%3Cpath d='M 120 240 L 380 240' stroke='%23cbd5e1' stroke-width='1' opacity='0.2'/%3E%3Ccircle cx='150' cy='270' r='25' fill='url(%23grad1)' opacity='0.8'/%3E%3Cpath d='M 145 270 L 150 275 L 160 265' stroke='%23ffffff' stroke-width='3' fill='none' stroke-linecap='round' stroke-linejoin='round'/%3E%3Ctext x='190' y='275' font-family='Arial' font-size='14' fill='%23cbd5e1'%3EReal-time Analytics%3C/text%3E%3Ccircle cx='150' cy='310' r='25' fill='url(%23grad2)' opacity='0.8'/%3E%3Cpath d='M 145 310 L 150 315 L 160 305' stroke='%23ffffff' stroke-width='3' fill='none' stroke-linecap='round' stroke-linejoin='round'/%3E%3Ctext x='190' y='315' font-family='Arial' font-size='14' fill='%23cbd5e1'%3ESecure Payments%3C/text%3E%3Ccircle cx='30' cy='100' r='8' fill='%2322d3ee' opacity='0.6'%3E%3Canimate attributeName='opacity' values='0.6;1;0.6' dur='2s' repeatCount='indefinite'/%3E%3C/circle%3E%3Ccircle cx='460' cy='200' r='6' fill='%238b5cf6' opacity='0.5'%3E%3Canimate attributeName='opacity' values='0.5;1;0.5' dur='3s' repeatCount='indefinite'/%3E%3C/circle%3E%3Ccircle cx='50' cy='300' r='5' fill='%2306b6d4' opacity='0.7'%3E%3Canimate attributeName='opacity' values='0.7;1;0.7' dur='2.5s' repeatCount='indefinite'/%3E%3C/circle%3E%3C/svg%3E" alt="Sales Management Illustration">
                </div>
            </div>
        </section>

        <!-- Why Choose Section -->
        <section class="why-choose-section" id="why-choose">
            <div class="section-header">
                <h2 class="section-title">
                    Why Choose <span class="gradient-highlight">BIREENA SALESY</span>
                </h2>
                <p class="section-subtitle">
                    Manage sales, inventory, and employee performance in one easy-to-use platform built for modern shops.
                </p>
            </div>

            <div class="features-grid">
                <!-- Dashboard Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <line x1="18" y1="20" x2="18" y2="10"></line>
                                <line x1="12" y1="20" x2="12" y2="4"></line>
                                <line x1="6" y1="20" x2="6" y2="14"></line>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Dashboard</h3>
                            <p class="feature-description">Get a real-time view of total sales, expenses, and key business metrics in one central dashboard.</p>
                        </div>
                    </div>
                </div>

                <!-- Inventory Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
                                <polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
                                <line x1="12" y1="22.08" x2="12" y2="12"></line>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Inventory</h3>
                            <p class="feature-description">Track stock levels, low-stock alerts, and product performance to avoid missing critical items.</p>
                        </div>
                    </div>
                </div>

                <!-- Sales Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
                                <line x1="3" y1="6" x2="21" y2="6"></line>
                                <path d="M16 10a4 4 0 0 1-8 0"></path>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Sales</h3>
                            <p class="feature-description">Create fast, accurate bills with discounts, taxes, and multiple payment methods in seconds.</p>
                        </div>
                    </div>
                </div>

                <!-- Reports Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="3" width="7" height="7"></rect>
                                <rect x="14" y="3" width="7" height="7"></rect>
                                <rect x="14" y="14" width="7" height="7"></rect>
                                <rect x="3" y="14" width="7" height="7"></rect>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Reports</h3>
                            <p class="feature-description">Generate daily, weekly, and monthly reports for sales, expenses, and profit analysis.</p>
                        </div>
                    </div>
                </div>

                <!-- Employees Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                                <circle cx="9" cy="7" r="4"></circle>
                                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Employees</h3>
                            <p class="feature-description">Manage employee logins, roles, and performance to keep your team productive and accountable.</p>
                        </div>
                    </div>
                </div>

                <!-- Discount Card -->
                <div class="feature-card">
                    <div class="feature-card-content">
                        <div class="feature-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"></path>
                                <line x1="7" y1="7" x2="7.01" y2="7"></line>
                            </svg>
                        </div>
                        <div class="feature-text">
                            <h3 class="feature-title">Discount</h3>
                            <p class="feature-description">Configure flexible offers and discounts to boost sales while keeping control of margins.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Meet Our Owner Section -->
        <section class="owner-section" id="owner">
            <div class="owner-container">
                <div class="owner-image-wrapper">
                    <img src="/components/hr.png" alt="Owner of Bireena Info Tech" class="owner-image">
                </div>
                <div class="owner-content">
                    <h2 class="owner-heading">Meet Our Owner</h2>
                    <p class="owner-subheading">Passionate about culinary excellence and cultural fusion</p>
                    
                    <p class="owner-text">
                        With over 15 years of culinary expertise, our owner brings a unique blend of traditional recipes and modern innovations to BIREENA अतिथि.
                    </p>
                    
                    <p class="owner-text">
                        Their journey began in the heart of India, learning ancestral cooking techniques and secret family recipes. Today, they continue to innovate while preserving the authentic flavors that make Indian cuisine special.
                    </p>
                    
                    <p class="owner-quote">
                        "My goal is to create not just a restaurant, but a cultural experience where every guest feels like family."
                    </p>
                    
                    <div class="social-links">
                        <a href="https://www.instagram.com/bireenainfo/" target="_blank" rel="noreferrer" class="social-btn instagram">
                            <span class="social-icon">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                                    <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                                    <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                                </svg>
                            </span>
                            Instagram
                        </a>
                        
                        <a href="https://www.facebook.com/people/Bireena-Bireena/61572904348705/" target="_blank" rel="noreferrer" class="social-btn facebook">
                            <span class="social-icon">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path>
                                </svg>
                            </span>
                            Facebook
                        </a>
                        
                        <a href="https://www.youtube.com/@Bireenainfotech" target="_blank" rel="noreferrer" class="social-btn youtube">
                            <span class="social-icon">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"></path>
                                    <polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"></polygon>
                                </svg>
                            </span>
                            YouTube
                        </a>
                    </div>
                </div>
            </div>
        </section>

        <!-- Contact Section -->
        <section class="contact-section" id="contact">
            <div class="contact-header">
                <h2 class="contact-title">Get in Touch</h2>
                <p class="contact-subtitle">
                    Have a question about BIREENA SALESY? Send us a message and we'll get back to you soon.
                </p>
            </div>

            <div class="contact-card">
                <form id="contactForm" onsubmit="handleContactSubmit(event)">
                    <div class="form-group">
                        <label class="form-label" for="contactName">Your Name</label>
                        <input 
                            type="text" 
                            id="contactName" 
                            name="name"
                            class="form-input" 
                            placeholder="Enter your full name"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="contactEmail">Email Address</label>
                        <input 
                            type="email" 
                            id="contactEmail" 
                            name="email"
                            class="form-input" 
                            placeholder="your.email@example.com"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="contactSubject">Subject</label>
                        <input 
                            type="text" 
                            id="contactSubject" 
                            name="subject"
                            class="form-input" 
                            placeholder="What is this regarding?"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="contactMessage">Message</label>
                        <textarea 
                            id="contactMessage" 
                            name="message"
                            class="form-textarea" 
                            placeholder="Tell us more about your inquiry..."
                            required
                        ></textarea>
                    </div>

                    <button type="submit" class="submit-btn" id="submitBtn">
                        Send Message
                    </button>

                    <div id="formMessage" class="form-message"></div>
                </form>
            </div>
        </section>

        <!-- Footer Section -->
        <footer class="footer-section" id="footer">
            <div class="footer-container">
                <div class="footer-grid">
                    <!-- Brand & Contact Info -->
                    <div class="footer-column">
                        <div class="footer-brand">
                            BIREENA<span class="gradient-text">SALESY</span>
                        </div>
                        <div class="footer-info">
                            <div class="footer-info-item">
                                <svg class="footer-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                                    <circle cx="12" cy="10" r="3"></circle>
                                </svg>
                                <span>B-36, Anisabad, Patna, Bihar, India 8000025</span>
                            </div>
                            <div class="footer-info-item">
                                <svg class="footer-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
                                </svg>
                                <span>+91 91351-55931 , +91 93049-42225</span>
                            </div>
                            <div class="footer-info-item">
                                <svg class="footer-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                                    <polyline points="22,6 12,13 2,6"></polyline>
                                </svg>
                                <span>bireenainfo@gmail.com</span>
                            </div>
                        </div>
                        <div class="footer-social">
                            <a href="https://www.instagram.com/bireenainfo/" target="_blank" rel="noreferrer" class="social-icon-link">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                                    <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                                    <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                                </svg>
                            </a>
                            <a href="https://www.facebook.com/people/Bireena-Bireena/61572904348705/" target="_blank" rel="noreferrer" class="social-icon-link">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path>
                                </svg>
                            </a>
                            <a href="https://www.youtube.com/@Bireenainfotech" target="_blank" rel="noreferrer" class="social-icon-link">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"></path>
                                    <polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"></polygon>
                                </svg>
                            </a>
                        </div>
                    </div>

                    <!-- Services Links -->
                    <div class="footer-column">
                        <div class="footer-card">
                            <h3 class="footer-title">Services</h3>
                            <div class="footer-links">
                                <a href="/about" class="footer-link">About Us</a>
                                <a href="/features" class="footer-link">Features</a>
                                <a href="/pricing" class="footer-link">Pricing</a>
                                <a href="/contact" class="footer-link">Contact</a>
                            </div>
                        </div>
                    </div>

                    <!-- Support Links -->
                    <div class="footer-column">
                        <div class="footer-card">
                            <h3 class="footer-title">Support</h3>
                            <div class="footer-links">
                                <a href="/contact" class="footer-link">Contact</a>
                                <a href="#" class="footer-link">FAQ</a>
                                <a href="#" class="footer-link">Privacy Policy</a>
                                <a href="#" class="footer-link">Terms of Service</a>
                            </div>
                        </div>
                    </div>

                    <!-- Opening Hours -->
                    <div class="footer-column">
                        <div class="footer-card">
                            <h3 class="footer-title">Opening Hours</h3>
                            <div class="hours-content">
                                <div class="hours-item">
                                    <span class="hours-day">Monday - Friday</span>
                                    <span class="hours-time">11:00 AM - 10:00 PM</span>
                                </div>
                                <div class="hours-item">
                                    <span class="hours-day">Saturday - Sunday</span>
                                    <span class="hours-time">12:00 PM - 11:00 PM</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="footer-bottom">
                    <p class="copyright">
                        © 2025 <strong>BIREENA SALESY</strong>. All rights reserved. Designed & Developed by <a href="https://bireenaatithi.in" target="_blank">Bireena Infotech</a>
                    </p>
                </div>
            </div>
        </footer>
    </div>

    <script>
        let currentSlide = 0;
        let autoSlideInterval;
        const testimonials = 8;
        let cardsPerView = 3;

        function updateCardsPerView() {
            if (window.innerWidth <= 768) {
                cardsPerView = 1;
            } else if (window.innerWidth <= 1024) {
                cardsPerView = 2;
            } else {
                cardsPerView = 3;
            }
        }

        function createDots() {
            const dotsContainer = document.getElementById('dotsContainer');
            dotsContainer.innerHTML = '';
            const totalSlides = Math.ceil(testimonials / cardsPerView);
            for (let i = 0; i < totalSlides; i++) {
                const dot = document.createElement('div');
                dot.className = 'dot' + (i === 0 ? ' active' : '');
                dot.onclick = () => goToSlide(i);
                dotsContainer.appendChild(dot);
            }
        }

        function updateSlider() {
            const track = document.getElementById('sliderTrack');
            const slideWidth = 100 / cardsPerView;
            track.style.transform = `translateX(-${currentSlide * 100}%)`;
            
            // Update dots
            const dots = document.querySelectorAll('.dot');
            dots.forEach((dot, index) => {
                dot.classList.toggle('active', index === currentSlide);
            });
        }

        function moveSlide(direction) {
            updateCardsPerView();
            const maxSlides = Math.ceil(testimonials / cardsPerView) - 1;
            currentSlide += direction;
            
            if (currentSlide < 0) {
                currentSlide = maxSlides;
            } else if (currentSlide > maxSlides) {
                currentSlide = 0;
            }
            
            updateSlider();
            resetAutoSlide();
        }

        function goToSlide(index) {
            currentSlide = index;
            updateSlider();
            resetAutoSlide();
        }

        function startAutoSlide() {
            autoSlideInterval = setInterval(() => {
                moveSlide(1);
            }, 5000);
        }

        function resetAutoSlide() {
            clearInterval(autoSlideInterval);
            startAutoSlide();
        }

        // Pause on hover
        const sliderContainer = document.querySelector('.slider-container');
        sliderContainer.addEventListener('mouseenter', () => {
            clearInterval(autoSlideInterval);
        });
        
        sliderContainer.addEventListener('mouseleave', () => {
            startAutoSlide();
        });

        // Touch/Swipe functionality for mobile
        let touchStartX = 0;
        let touchEndX = 0;
        let touchStartY = 0;
        let touchEndY = 0;

        const sliderWrapper = document.querySelector('.slider-wrapper');
        
        sliderWrapper.addEventListener('touchstart', (e) => {
            touchStartX = e.changedTouches[0].screenX;
            touchStartY = e.changedTouches[0].screenY;
            clearInterval(autoSlideInterval);
        }, { passive: true });

        sliderWrapper.addEventListener('touchend', (e) => {
            touchEndX = e.changedTouches[0].screenX;
            touchEndY = e.changedTouches[0].screenY;
            handleSwipe();
            startAutoSlide();
        }, { passive: true });

        function handleSwipe() {
            const swipeThreshold = 50;
            const diffX = touchStartX - touchEndX;
            const diffY = Math.abs(touchStartY - touchEndY);
            
            // Only register horizontal swipes
            if (Math.abs(diffX) > swipeThreshold && diffY < swipeThreshold) {
                if (diffX > 0) {
                    // Swiped left - go to next
                    moveSlide(1);
                } else {
                    // Swiped right - go to previous
                    moveSlide(-1);
                }
            }
        }

        // Initialize
        window.addEventListener('resize', () => {
            updateCardsPerView();
            createDots();
            if (currentSlide >= Math.ceil(testimonials / cardsPerView)) {
                currentSlide = 0;
            }
            updateSlider();
        });

        updateCardsPerView();
        createDots();
        updateSlider();
        startAutoSlide();

        document.addEventListener('DOMContentLoaded', function() {
            initMobileMenu();
        });

        // Mobile Menu Toggle Function
        function initMobileMenu() {
            const hamburgerBtn = document.getElementById('hamburgerBtn');
            const navMenu = document.getElementById('navMenu');
            const navOverlay = document.getElementById('navOverlay');
            const navLinks = navMenu.querySelectorAll('.nav-link');
            
            // Toggle menue) {
                e.preventDefault();
                e.stopPropagation();
                const isActive = navMenu.classList.toggle('active');
                navOverlay.classList.toggle('active');
                hamburgerBtn.classList.toggle('active');
                hamburgerBtn.setAttribute('aria-expanded', isActive);
                
                // Prevent body scroll when menu is open
                document.body.style.overflow = isActive ? 'hidden' : '';
            }
            
            // Close menu
            function closeMenu(e) {
                if (e) {
                    e.preventDefault();
                    e.stopPropagation();
                }
                navMenu.classList.remove('active');
                navOverlay.classList.remove('active');
                hamburgerBtn.classList.remove('active');
                hamburgerBtn.setAttribute('aria-expanded', 'false');
                document.body.style.overflow = '';
            }
            
            // Fast mobile touch handling
            let touchStartTime = 0;
            hamburgerBtn.addEventListener('touchstart', (e) => {
                touchStartTime = Date.now();
                toggleMenu(e);
            }, { passive: false });
            
            // Fallback for desktop/mouse
            hamburgerBtn.addEventListener('click', (e) => {
                // Prevent double trigger on mobile (touchstart already handled)
                if (Date.now() - touchStartTime > 300) {
                    toggleMenu(e);
                }
            });
            
            navOverlay.addEventListener('touchstart', closeMenu, { passive: false }
            hamburgerBtn.addEventListener('click', toggleMenu);
            navOverlay.addEventListener('click', closeMenu);
            
            // Close menu when clicking nav links
            navLinks.forEach(link => {
                link.addEventListener('click', () => {
                    if (window.innerWidth <= 1024) {
                        closeMenu();
                    }
                });
            });
            
            // Close menu on escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && navMenu.classList.contains('active')) {
                    closeMenu();
                    hamburgerBtn.focus();
                }
            });
            
            // Handle window resize
            let resizeTimer;
            window.addEventListener('resize', () => {
                clearTimeout(resizeTimer);
                resizeTimer = setTimeout(() => {
                    if (window.innerWidth > 1024) {
                        closeMenu();
                    }
                }, 250);
            });
        }

        // Smooth Scroll Function with fixed header offset
        function smoothScrollTo(sectionId) {
            const section = document.getElementById(sectionId);
            if (section) {
                const headerHeight = document.querySelector('.top-navigation').offsetHeight;
                const sectionTop = section.getBoundingClientRect().top + window.pageYOffset - headerHeight;
                
                window.scrollTo({
                    top: sectionTop,
                    behavior: 'smooth'
                });
            }
        }

        // Contact Form Handler
        async function handleContactSubmit(event) {
            event.preventDefault();
            
            const form = event.target;
            const submitBtn = document.getElementById('submitBtn');
            const formMessage = document.getElementById('formMessage');
            
            // Get form data
            const formData = {
                name: form.name.value.trim(),
                email: form.email.value.trim(),
                subject: form.subject.value.trim(),
                message: form.message.value.trim()
            };
            
            // Validate
            if (!formData.name || !formData.email || !formData.subject || !formData.message) {
                showMessage('Please fill in all fields', 'error');
                return;
            }
            
            // Email validation
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(formData.email)) {
                showMessage('Please enter a valid email address', 'error');
                return;
            }
            
            // Show loading
            submitBtn.disabled = true;
            submitBtn.textContent = 'Sending...';
            formMessage.style.display = 'none';
            
            try {
                const response = await fetch('/api/contact', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });
                
                const data = await response.json();
                
                if (response.ok && data.success) {
                    showMessage('Message sent successfully! We will get back to you soon.', 'success');
                    form.reset();
                } else {
                    showMessage(data.message || 'Failed to send message. Please try again.', 'error');
                }
            } catch (error) {
                console.error('Contact form error:', error);
                showMessage('An error occurred. Please try again later.', 'error');
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = 'Send Message';
            }
        }
        
        function showMessage(text, type) {
            const formMessage = document.getElementById('formMessage');
            formMessage.textContent = text;
            formMessage.className = `form-message ${type}`;
            formMessage.style.display = 'block';
            
            if (type === 'success') {
                setTimeout(() => {
                    formMessage.style.display = 'none';
                }, 5000);
            }
        }
    </script>
</body>
</html>
