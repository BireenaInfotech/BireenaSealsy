<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expired Products - Bireena Bakery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container-fluid mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2><i class="bi bi-x-circle text-danger"></i> Expired Products</h2>
            <a href="/inventory" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to Inventory
            </a>
        </div>

        <div class="alert alert-danger">
            <i class="bi bi-exclamation-octagon"></i> 
            <strong>Critical:</strong> These products have expired. Remove them from inventory immediately!
        </div>

        <div class="card">
            <div class="card-body">
                <% if (products.length === 0) { %>
                    <div class="text-center py-5">
                        <i class="bi bi-check-circle text-success" style="font-size: 3rem;"></i>
                        <h4 class="mt-3">No Expired Products</h4>
                        <p class="text-muted">Great! Your inventory is up to date.</p>
                    </div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>PRODUCT</th>
                                    <th>CATEGORY</th>
                                    <th>STOCK</th>
                                    <th>EXPIRY DATE</th>
                                    <th>EXPIRED SINCE</th>
                                    <th>ACTIONS</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% products.forEach(product => { 
                                    const today = new Date();
                                    const expiry = new Date(product.expiryDate);
                                    const daysExpired = Math.abs(Math.ceil((expiry - today) / (1000 * 60 * 60 * 24)));
                                %>
                                    <tr class="table-danger">
                                        <td><strong><%= product.name %></strong></td>
                                        <td><%= product.category %></td>
                                        <td><strong class="text-danger"><%= product.stock %></strong> <%= product.unit %></td>
                                        <td><%= new Date(product.expiryDate).toLocaleDateString() %></td>
                                        <td>
                                            <span class="badge bg-danger">
                                                <%= daysExpired %> days ago
                                            </span>
                                        </td>
                                        <td>
                                            <a href="/inventory/damage/<%= product._id %>" class="btn btn-sm btn-danger">
                                                <i class="bi bi-trash"></i> Remove as Damage
                                            </a>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
