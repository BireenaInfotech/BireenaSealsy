<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expiring Soon - Bireena Bakery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container-fluid mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2><i class="bi bi-calendar-event"></i> Products Expiring Soon (30 Days)</h2>
            <a href="/inventory" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to Inventory
            </a>
        </div>

        <div class="alert alert-warning">
            <i class="bi bi-exclamation-triangle"></i> 
            <strong>Alert:</strong> These products will expire within the next 30 days. Take action soon!
        </div>

        <div class="card">
            <div class="card-body">
                <% if (products.length === 0) { %>
                    <div class="text-center py-5">
                        <i class="bi bi-check-circle text-success" style="font-size: 3rem;"></i>
                        <h4 class="mt-3">No Products Expiring Soon</h4>
                        <p class="text-muted">All products are fresh!</p>
                    </div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>PRODUCT</th>
                                    <th>CATEGORY</th>
                                    <th>STOCK</th>
                                    <th>MFG DATE</th>
                                    <th>EXPIRY DATE</th>
                                    <th>DAYS LEFT</th>
                                    <th>ACTIONS</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% products.forEach(product => { 
                                    const today = new Date();
                                    const expiry = new Date(product.expiryDate);
                                    const daysLeft = Math.ceil((expiry - today) / (1000 * 60 * 60 * 24));
                                    let urgencyClass = '';
                                    if (daysLeft <= 7) urgencyClass = 'table-danger';
                                    else if (daysLeft <= 15) urgencyClass = 'table-warning';
                                %>
                                    <tr class="<%= urgencyClass %>">
                                        <td><strong><%= product.name %></strong></td>
                                        <td><%= product.category %></td>
                                        <td><strong><%= product.stock %></strong> <%= product.unit %></td>
                                        <td>
                                            <% if (product.mfgDate) { %>
                                                <%= new Date(product.mfgDate).toLocaleDateString() %>
                                            <% } else { %>
                                                <span class="text-muted">N/A</span>
                                            <% } %>
                                        </td>
                                        <td><%= new Date(product.expiryDate).toLocaleDateString() %></td>
                                        <td>
                                            <span class="badge <%= daysLeft <= 7 ? 'bg-danger' : 'bg-warning' %>">
                                                <%= daysLeft %> days
                                            </span>
                                        </td>
                                        <td>
                                            <a href="/inventory/edit/<%= product._id %>" class="btn btn-sm btn-primary">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a href="/inventory/damage/<%= product._id %>" class="btn btn-sm btn-warning">
                                                <i class="bi bi-trash"></i> Damage
                                            </a>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
