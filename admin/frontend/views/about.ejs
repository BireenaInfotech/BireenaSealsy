<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
    <title>About - Bireena Salesy</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
        :root {
            --primary-purple: #7c3aed;
            --primary-purple-dark: #6d28d9;
            --secondary-pink: #ec4899;
            --accent-cyan: #06b6d4;
            --accent-cyan-light: #22d3ee;
            --dark-bg: #0f0318;
            --dark-bg-alt: #1a0b2e;
            --text-primary: #ffffff;
            --text-secondary: #cbd5e1;
            --text-muted: #94a3b8;
            --font-heading: 'Playfair Display', serif;
            --font-body: 'Poppins', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            overflow-x: hidden;
            width: 100%;
        }

        body {
            font-family: var(--font-body);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
            width: 100%;
            max-width: 100vw;
            background: 
                radial-gradient(circle at 20% 50%, rgba(124, 58, 237, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(236, 72, 153, 0.3) 0%, transparent 50%),
                linear-gradient(135deg, #0f0318 0%, #1a0b2e 50%, #2d1b4e 100%);
            background-attachment: fixed;
        }

        /* Global Image and Container Overflow Fix */
        img, svg, video {
            max-width: 100%;
            height: auto;
        }

        section, div {
            max-width: 100%;
        }
        
        /* Ensure all containers respect box-sizing */
        .journey-card,
        .feature-card,
        .team-card,
        .hero-content,
        .journey-content {
            box-sizing: border-box;
            max-width: 100%;
        }
        
        /* Prevent horizontal overflow */
        .features-grid,
        .team-grid {
            width: 100%;
            max-width: 100%;
        }

        /* Navigation */
        :root {
            --header-height: 75px;
        }

        .top-navigation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 0 2rem;
            z-index: 1000;
            background: rgba(15, 3, 24, 0.98);
            backdrop-filter: blur(15px);
            border-bottom: 1px solid rgba(124, 58, 237, 0.15);
            height: var(--header-height);
            display: flex;
            align-items: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }

        .nav-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1400px;
            margin: 0 auto;
            width: 100%;
            height: 100%;
        }

        .site-title {
            font-size: 1.4rem;
            font-weight: 700;
            letter-spacing: 0.3px;
            color: var(--text-primary);
            font-family: var(--font-heading);
            cursor: pointer;
            z-index: 1001;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.25rem;
        }

        .site-title .gradient-text {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--primary-purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Hamburger Menu Button */
        .hamburger-menu {
            display: none;
            flex-direction: column;
            gap: 5px;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            z-index: 1001;
            transition: transform 0.3s ease;
            touch-action: manipulation;
            -webkit-tap-highlight-color: transparent;
            position: relative;
            min-width: 44px;
            min-height: 44px;
        }

        .hamburger-menu:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
            border-radius: 4px;
        }

        .hamburger-line {
            width: 25px;
            height: 3px;
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            border-radius: 3px;
            transition: all 0.3s ease;
            pointer-events: none;
            display: block;
        }

        .hamburger-menu.active .hamburger-line:nth-child(1) {
            transform: translateY(8px) rotate(45deg);
        }

        .hamburger-menu.active .hamburger-line:nth-child(2) {
            opacity: 0;
        }

        .hamburger-menu.active .hamburger-line:nth-child(3) {
            transform: translateY(-8px) rotate(-45deg);
        }

        /* Mobile Overlay */
        .nav-overlay {
            position: fixed;
            top: var(--header-height);
            left: 0;
            width: 100%;
            height: calc(100vh - var(--header-height));
            background: rgba(0, 0, 0, 0.7);
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s ease, visibility 0.3s ease;
            z-index: 999;
        }

        .nav-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .nav-menu {
            display: flex;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link:hover, .nav-link.active {
            color: var(--text-primary);
        }

        .nav-link:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
            border-radius: 4px;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--accent-cyan), var(--primary-purple));
            transition: width 0.3s ease;
        }

        .nav-link:hover::after, .nav-link.active::after {
            width: 100%;
        }

        .nav-login-btn {
            padding: 0.65rem 2rem;
            background: linear-gradient(135deg, #8b5cf6 0%, #06b6d4 100%);
            color: #ffffff;
            border: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.95rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
            white-space: nowrap;
        }

        .nav-login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 25px rgba(139, 92, 246, 0.5);
        }

        .nav-login-btn:focus {
            outline: 2px solid var(--accent-cyan);
            outline-offset: 4px;
        }

        .nav-links a {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
            position: relative;
        }

        .nav-links a.active {
            color: var(--accent-cyan);
        }

        .nav-links a.active::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--accent-cyan);
        }

        .nav-links a:hover {
            color: var(--accent-cyan);
        }

        /* Hero Section */
        .hero-section {
            position: relative;
            margin-top: 100px;
            padding: 4rem 3rem 2rem 3rem;
            max-width: 1400px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-badge {
            display: inline-block;
            background: rgba(6, 182, 212, 0.1);
            border: 1px solid rgba(6, 182, 212, 0.3);
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            color: var(--accent-cyan);
            font-size: 0.9rem;
            font-weight: 500;
            margin-bottom: 2rem;
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            margin-bottom: 0;
        }

        .hero-text h1 {
            font-family: var(--font-heading);
            font-size: 3.5rem;
            line-height: 1.2;
            margin-bottom: 1.5rem;
        }

        .hero-text h1 .highlight {
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-text p {
            font-size: 1.1rem;
            color: var(--text-secondary);
            margin-bottom: 2rem;
            line-height: 1.8;
        }

        .trust-badge {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-top: 3rem;
        }

        .trust-icons {
            display: flex;
            gap: -15px;
        }

        .trust-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-purple), var(--secondary-pink));
            border: 3px solid var(--dark-bg);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: white;
        }

        .trust-text {
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        .trust-text strong {
            color: var(--accent-cyan);
            font-weight: 600;
        }

        .hero-image {
            position: relative;
            height: 500px;
            background: rgba(124, 58, 237, 0.1);
            border-radius: 20px;
            border: 1px solid rgba(124, 58, 237, 0.2);
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .stats-badge {
            position: absolute;
            bottom: 20px;
            right: 20px;
            background: rgba(15, 3, 24, 0.95);
            backdrop-filter: blur(10px);
            padding: 1rem 1.5rem;
            border-radius: 12px;
            border: 1px solid rgba(6, 182, 212, 0.4);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
        }

        .stats-badge h3 {
            color: var(--accent-cyan);
            font-size: 1.5rem;
            margin-bottom: 0.2rem;
        }

        .stats-badge p {
            color: var(--text-secondary);
            font-size: 0.85rem;
        }

        /* Scroll Down Arrow */
        .scroll-arrow {
            position: absolute;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: rgba(30, 15, 50, 0.8);
            backdrop-filter: blur(10px);
            border: 2px solid rgba(124, 58, 237, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            z-index: 10;
            animation: bounce 2s infinite;
        }

        .scroll-arrow:hover {
            transform: translateX(-50%) scale(1.1);
            border-color: var(--accent-cyan);
            box-shadow: 0 0 20px rgba(6, 182, 212, 0.6);
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(124, 58, 237, 0.2));
        }

        .scroll-arrow svg {
            color: var(--accent-cyan);
            font-size: 1.5rem;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateX(-50%) translateY(0);
            }
            40% {
                transform: translateX(-50%) translateY(-10px);
            }
            60% {
                transform: translateX(-50%) translateY(-5px);
            }
        }

        .scroll-arrow:hover {
            animation: none;
        }

        /* Journey Section */
        .journey-section {
            position: relative;
            padding: 2rem 3rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            font-family: var(--font-heading);
            font-size: 3rem;
            margin-bottom: 1.5rem;
        }

        .section-title .highlight {
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .journey-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .journey-text {
            color: var(--text-secondary);
            font-size: 1.05rem;
            line-height: 1.8;
        }

        .journey-text p {
            margin-bottom: 1.5rem;
        }

        .journey-card {
            background: rgba(124, 58, 237, 0.05);
            border: 1px solid rgba(124, 58, 237, 0.2);
            border-radius: 20px;
            padding: 2.5rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .journey-card:hover {
            transform: translateY(-5px);
            border-color: var(--accent-cyan);
            box-shadow: 0 15px 35px rgba(6, 182, 212, 0.3);
            background: rgba(124, 58, 237, 0.08);
        }

        .journey-image {
            height: 400px;
            background: rgba(6, 182, 212, 0.1);
            border-radius: 15px;
            border: 1px solid rgba(6, 182, 212, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-muted);
        }

        /* Features Section */
        .features-section {
            position: relative;
            padding: 2rem 3rem;
            max-width: 1400px;
            margin: 0 auto;
            text-align: center;
        }

        .features-intro {
            max-width: 800px;
            margin: 0 auto 2rem;
        }

        .features-intro h2 {
            font-family: var(--font-heading);
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .features-intro p {
            color: var(--text-secondary);
            font-size: 1.1rem;
            line-height: 1.8;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 2rem;
        }

        .feature-card {
            background: rgba(124, 58, 237, 0.05);
            border: 1px solid rgba(124, 58, 237, 0.2);
            border-radius: 20px;
            padding: 2.5rem;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            border-color: var(--accent-cyan);
            box-shadow: 0 20px 40px rgba(6, 182, 212, 0.2);
        }

        .feature-icon {
            width: 70px;
            height: 70px;
            margin: 0 auto 1.5rem;
            background: linear-gradient(135deg, var(--primary-purple), var(--secondary-pink));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
        }

        .feature-card h3 {
            font-size: 1.3rem;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .feature-card p {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.7;
        }

        /* Team Section */
        .team-section {
            position: relative;
            padding: 2rem 3rem;
            max-width: 1400px;
            margin: 0 auto;
            text-align: center;
        }

        .team-intro h2 {
            font-family: var(--font-heading);
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .team-intro p {
            color: var(--text-secondary);
            font-size: 1.1rem;
            max-width: 700px;
            margin: 0 auto 2rem;
        }

        .team-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
        }

        @media (max-width: 992px) {
            .team-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 576px) {
            .team-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
        }

        .team-card {
            background: rgba(124, 58, 237, 0.05);
            border: 1px solid rgba(124, 58, 237, 0.2);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .team-card:hover {
            transform: translateY(-10px);
            border-color: var(--accent-cyan);
            box-shadow: 0 20px 40px rgba(6, 182, 212, 0.2);
        }

        .team-image {
            height: 300px;
            background: linear-gradient(135deg, rgba(124, 58, 237, 0.3), rgba(236, 72, 153, 0.3));
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-muted);
            font-size: 0.9rem;
            overflow: hidden;
            position: relative;
        }

        .team-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center top;
            transition: transform 0.3s ease;
        }

        /* Specific adjustments for each card */
        .team-card:nth-child(1) .team-image img {
            object-position: center 30%;
        }

        .team-card:nth-child(2) .team-image img {
            object-position: center 25%;
        }

        .team-card:nth-child(3) .team-image img {
            object-position: center 35%;
        }

        .team-card:hover .team-image img {
            transform: scale(1.1);
        }

        .team-info {
            padding: 2rem;
            text-align: center;
        }

        .team-info h3 {
            font-size: 1.3rem;
            margin-bottom: 0.5rem;
        }

        .team-info .role {
            color: var(--accent-cyan);
            font-size: 0.95rem;
            margin-bottom: 1rem;
            font-weight: 500;
        }

        .team-info p {
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.6;
        }

        .team-social {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 1rem;
        }

        .social-icon {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            background: rgba(6, 182, 212, 0.1);
            border: 1px solid rgba(6, 182, 212, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent-cyan);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .social-icon:hover {
            background: var(--accent-cyan);
            color: white;
            transform: translateY(-3px);
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, #0a0515 0%, #0d0820 50%, #1a0b2e 100%);
            border-top: 1px solid rgba(124, 58, 237, 0.3);
            padding: 4rem 3rem 2rem;
            margin-top: 5rem;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1.5fr;
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-card {
            background: rgba(15, 6, 40, 0.6);
            border: 1px solid rgba(124, 58, 237, 0.3);
            border-radius: 16px;
            padding: 2rem;
            backdrop-filter: blur(10px);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            height: 100%;
        }

        .footer-card:hover {
            background: rgba(20, 10, 50, 0.8);
            border-color: rgba(6, 182, 212, 0.6);
            box-shadow: 0 8px 32px rgba(6, 182, 212, 0.2),
                        0 0 40px rgba(124, 58, 237, 0.15);
            transform: translateY(-5px);
        }

        .footer-brand h3 {
            font-size: 1.8rem;
            margin-bottom: 1rem;
        }

        .footer-brand .highlight {
            color: var(--accent-cyan);
        }

        .footer-contact {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .contact-item {
            display: flex;
            align-items: flex-start;
            gap: 0.8rem;
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        .contact-icon {
            color: var(--accent-cyan);
            margin-top: 3px;
        }

        .footer-section h4 {
            color: #ffffff;
            margin-bottom: 1.5rem;
            font-size: 1.25rem;
            font-weight: 600;
            letter-spacing: 0.3px;
        }

        .footer-links {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
            padding-left: 1.25rem;
        }

        .footer-links a::before {
            content: '→';
            color: var(--accent-cyan);
            position: absolute;
            left: 0;
            opacity: 0;
            transform: translateX(-5px);
            transition: all 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--accent-cyan);
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.3);
            padding-left: 1.5rem;
        }

        .footer-links a:hover::before {
            opacity: 1;
            transform: translateX(0);
        }

        .footer-hours {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.8;
        }

        .footer-hours .time {
            color: var(--accent-cyan);
            font-weight: 600;
            font-size: 1rem;
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.3);
        }

        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .social-link {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(6, 182, 212, 0.1);
            border: 1px solid rgba(6, 182, 212, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent-cyan);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .social-link:hover {
            background: var(--accent-cyan);
            color: white;
            transform: translateY(-3px);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid rgba(124, 58, 237, 0.2);
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .footer-bottom a {
            color: var(--accent-cyan);
            text-decoration: none;
        }

        /* Scroll to Top Button */
        .scroll-to-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 55px;
            height: 55px;
            background: linear-gradient(135deg, var(--accent-cyan), var(--primary-purple));
            border: none;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transform: translateY(100px);
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 999;
            box-shadow: 
                0 8px 25px rgba(124, 58, 237, 0.4),
                0 0 20px rgba(6, 182, 212, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }

        .scroll-to-top.show {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .scroll-to-top:hover {
            transform: translateY(-5px) scale(1.1);
            box-shadow: 
                0 12px 35px rgba(124, 58, 237, 0.6),
                0 0 30px rgba(6, 182, 212, 0.5),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
        }

        .scroll-to-top:active {
            transform: translateY(-3px) scale(1.05);
        }

        .scroll-to-top svg {
            color: white;
            filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
            animation: arrowBounce 2s ease-in-out infinite;
        }

        @keyframes arrowBounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-5px);
            }
        }

        /* Responsive */
        /* Tablet (1024px and below) */
        @media (max-width: 1024px) {
            :root {
                --header-height: 70px;
            }

            .top-navigation {
                padding: 1rem 2rem;
            }

            .hamburger-menu {
                display: flex;
                order: 3;
            }

            .site-title {
                order: 1;
                font-size: 1.3rem;
            }

            .nav-login-btn {
                order: 2;
                padding: 0.5rem 1.5rem;
                font-size: 0.85rem;
            }

            .nav-menu {
                position: fixed;
                top: var(--header-height);
                left: 0;
                width: 100%;
                max-width: 400px;
                height: calc(100vh - var(--header-height));
                background: rgba(15, 3, 24, 0.98);
                backdrop-filter: blur(20px);
                flex-direction: column;
                justify-content: flex-start;
                padding: 2rem;
                gap: 0;
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                border-right: 1px solid rgba(124, 58, 237, 0.3);
                overflow-y: auto;
                box-shadow: 4px 0 20px rgba(0, 0, 0, 0.5);
                z-index: 1000;
            }

            .nav-menu.active {
                transform: translateX(0);
            }

            .nav-link {
                width: 100%;
                padding: 1rem;
                font-size: 1.1rem;
                border-bottom: 1px solid rgba(124, 58, 237, 0.2);
                text-align: left;
            }

            .nav-link::after {
                display: none;
            }

            .nav-login-btn {
                display: none !important;
            }

            .nav-link-mobile-login {
                display: block !important;
            }

            .journey-section > div {
                grid-template-columns: 1fr !important;
                gap: 2rem;
            }

            .features-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 1.5rem;
            }

            .team-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 1.5rem;
            }
        }

        /* Mobile (768px and below) */
        @media (max-width: 768px) {
            :root {
                --header-height: 65px;
            }

            .top-navigation {
                padding: 1rem 1.5rem;
            }

            .site-title {
                font-size: 1.2rem;
            }

            .nav-login-btn {
                padding: 0.5rem 1.2rem;
                font-size: 0.8rem;
            }

            .nav-menu {
                max-width: 100%;
            }
            
            /* Hero Section Mobile */
            .hero-section {
                padding: 3rem 1rem 2rem;
                margin-top: var(--header-height);
            }

            .hero-content {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .hero-text h1 {
                font-size: 2.5rem;
                line-height: 1.2;
            }
            
            .hero-text p {
                font-size: 1rem;
            }

            .hero-image {
                height: 300px;
                width: 100%;
            }

            .hero-badge {
                font-size: 0.85rem;
                padding: 0.4rem 1.25rem;
            }

            .trust-badge {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.75rem;
            }

            .section-title {
                font-size: 2.2rem;
                padding: 0 0.5rem;
                text-align: center;
            }
            
            /* Journey Section Mobile */
            .journey-section {
                padding: 2rem 1rem;
            }

            .journey-section > div {
                grid-template-columns: 1fr !important;
                gap: 1.5rem;
                max-width: 100%;
            }

            .journey-image {
                height: 250px;
                width: 100%;
            }

            .journey-card {
                padding: 1.75rem;
                width: 100%;
                max-width: 100%;
                box-sizing: border-box;
                height: auto !important;
                min-height: auto !important;
            }
            
            .journey-card h3 {
                font-size: 1.4rem;
            }
            
            .journey-card p {
                font-size: 0.95rem;
                line-height: 1.7;
            }
            
            /* Features Section Mobile */
            .features-section {
                padding: 2rem 1rem;
            }
            
            .features-intro {
                padding: 0 0.5rem;
                max-width: 100%;
                margin-bottom: 2rem;
            }
            
            .features-intro h2 {
                font-size: 2.2rem;
            }
            
            .features-intro p {
                font-size: 1rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                padding: 0;
            }
            
            .feature-card {
                padding: 2rem 1.5rem;
                width: 100%;
                max-width: 100%;
                box-sizing: border-box;
            }
            
            .feature-icon {
                width: 60px;
                height: 60px;
                font-size: 1.8rem;
            }
            
            .feature-card h3 {
                font-size: 1.2rem;
            }
            
            .feature-card p {
                font-size: 0.9rem;
            }
            
            /* Team Section Mobile */
            .team-section {
                padding: 2rem 1rem;
            }

            .team-intro {
                margin-bottom: 2rem;
            }

            .team-intro h2 {
                font-size: 2.2rem;
            }

            .team-intro p {
                font-size: 1rem;
                padding: 0 0.5rem;
            }

            .team-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                padding: 0;
            }
            
            .team-card {
                width: 100%;
                max-width: 100%;
                box-sizing: border-box;
            }

            .team-info {
                padding: 1.5rem;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .scroll-to-top {
                bottom: 20px;
                right: 20px;
                width: 50px;
                height: 50px;
            }
        }

        /* Small Mobile (640px and below) */
        @media (max-width: 640px) {
            :root {
                --header-height: 60px;
            }

            .top-navigation {
                padding: 0.75rem 1rem;
            }

            .logo {
                font-size: 1.2rem;
            }

            .login-btn {
                padding: 0.4rem 1rem;
                font-size: 0.8rem;
            }
            
            /* Hero Section Small Mobile */
            .hero-section {
                padding: 2.5rem 0.75rem 1.5rem;
            }

            .hero-text h1 {
                font-size: 2rem;
                line-height: 1.3;
            }

            .hero-text p {
                font-size: 0.95rem;
                line-height: 1.6;
            }

            .hero-badge {
                font-size: 0.75rem;
                padding: 0.35rem 1rem;
            }
            
            .hero-image {
                height: 250px;
            }

            .section-title {
                font-size: 2rem;
                padding: 0 0.5rem;
            }
            
            /* Journey Section Small Mobile */
            .journey-section {
                padding: 1.5rem 0.75rem;
            }

            .journey-card {
                padding: 1.5rem;
                border-radius: 15px;
            }

            .journey-card h3 {
                font-size: 1.25rem;
                margin-bottom: 1rem;
                padding-bottom: 0.7rem;
            }
            
            .journey-card p {
                font-size: 0.9rem;
                line-height: 1.6;
                margin-bottom: 1rem;
            }
            
            .journey-image {
                height: 200px;
                border-radius: 12px;
            }
            
            /* Features Section Small Mobile */
            .features-section {
                padding: 1.5rem 0.75rem;
            }
            
            .features-intro h2 {
                font-size: 2rem;
            }
            
            .features-intro p {
                font-size: 0.95rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 1.25rem;
            }
            
            .feature-card {
                padding: 1.75rem 1.25rem;
                border-radius: 15px;
            }
            
            .feature-icon {
                width: 55px;
                height: 55px;
                font-size: 1.5rem;
                margin-bottom: 1rem;
            }
            
            .feature-card h3 {
                font-size: 1.15rem;
                margin-bottom: 0.75rem;
            }
            
            .feature-card p {
                font-size: 0.875rem;
                line-height: 1.6;
            }
            
            /* Team Section Small Mobile */
            .team-section {
                padding: 1.5rem 0.75rem;
            }

            .team-intro h2 {
                font-size: 2rem;
            }

            .team-intro p {
                font-size: 0.95rem;
            }
            
            .team-grid {
                grid-template-columns: 1fr;
                gap: 1.25rem;
            }
            
            .team-card {
                border-radius: 15px;
            }

            .team-image {
                height: 250px;
            }

            .team-info {
                padding: 1.5rem;
            }

            .team-info h3 {
                font-size: 1.2rem;
            }

            .team-info .role {
                font-size: 0.9rem;
            }

            .team-info p {
                font-size: 0.875rem;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                padding: 0 0.75rem;
            }

            .footer-card {
                padding: 1.75rem;
            }

            .scroll-to-top {
                bottom: 15px;
                right: 15px;
                width: 45px;
                height: 45px;
            }
        }
        
        /* Extra Small Mobile (480px and below) */
        @media (max-width: 480px) {
            :root {
                --header-height: 60px;
            }

            .top-navigation {
                padding: 0.75rem 0.75rem;
            }

            .site-title {
                font-size: 0.95rem;
                word-spacing: -2px;
            }

            .nav-login-btn {
                padding: 0.4rem 0.9rem;
                font-size: 0.7rem;
            }

            .hero-text h1 {
                font-size: 1.75rem;
            }

            .hero-text p {
                font-size: 0.875rem;
            }

            .hero-image {
                height: 220px;
            }

            .stats-badge {
                padding: 0.75rem 1rem;
            }

            .stats-badge h3 {
                font-size: 1.25rem;
            }
            
            .section-title {
                font-size: 1.75rem;
            }
            
            .journey-card {
                padding: 1.25rem;
            }

            .journey-card h3 {
                font-size: 1.15rem;
            }
            
            .journey-card p {
                font-size: 0.85rem;
            }
            
            .journey-image {
                height: 180px;
            }
            
            .hero-section,
            .journey-section,
            .features-section,
            .team-section {
                padding: 1.25rem 0.5rem;
            }
            
            .features-intro h2 {
                font-size: 1.75rem;
            }

            .features-intro p {
                font-size: 0.875rem;
            }

            .feature-card {
                padding: 1.5rem 1rem;
            }
            
            .feature-icon {
                width: 50px;
                height: 50px;
                font-size: 1.35rem;
            }

            .feature-card h3 {
                font-size: 1.05rem;
            }

            .feature-card p {
                font-size: 0.8rem;
            }

            .team-intro h2 {
                font-size: 1.75rem;
            }

            .team-intro p {
                font-size: 0.875rem;
            }

            .team-card {
                border-radius: 12px;
            }

            .team-image {
                height: 220px;
            }

            .team-info {
                padding: 1.25rem;
            }

            .team-info h3 {
                font-size: 1.1rem;
            }

            .team-info .role {
                font-size: 0.85rem;
            }

            .team-info p {
                font-size: 0.8rem;
            }

            .footer-card {
                padding: 1.5rem;
            }

            .footer-brand h3 {
                font-size: 1.5rem;
            }

            .contact-item {
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation Overlay (for mobile) -->
    <div class="nav-overlay" id="navOverlay" aria-hidden="true"></div>
    
    <!-- Navigation -->
    <header class="top-navigation" role="banner">
        <div class="nav-container">
            <div class="site-title" onclick="window.location.href='/'" role="button" tabindex="0" aria-label="Bireena Salesy Home">
                BIREENA<span class="gradient-text">SALESY</span>
            </div>
            
            <nav class="nav-menu" id="navMenu" role="navigation" aria-label="Main navigation">
                <a href="/" class="nav-link">Home</a>
                <a href="/about" class="nav-link active" aria-current="page">About</a>
                <a href="/features" class="nav-link">Features</a>
                <a href="/pricing" class="nav-link">Pricing</a>
                <a href="/contact" class="nav-link">Contact</a>
                <a href="/login" class="nav-link nav-link-mobile-login" style="display: none; margin-top: 1rem; background: linear-gradient(135deg, #8b5cf6 0%, #06b6d4 100%); color: white; border-radius: 12px; text-align: center; border: none;">Login →</a>
            </nav>
            
            <button class="nav-login-btn" onclick="window.location.href='/login'" aria-label="Login">Login →</button>
            
            <button class="hamburger-menu" id="hamburgerBtn" aria-label="Toggle navigation menu" aria-expanded="false" aria-controls="navMenu">
                <span class="hamburger-line"></span>
                <span class="hamburger-line"></span>
                <span class="hamburger-line"></span>
            </button>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero-section">
        <span class="hero-badge">About Bireena Salesy</span>
        
        <div class="hero-content">
            <div class="hero-text">
                <h1>
                    Elevating Sales Management<br>
                    <span class="highlight">to New Heights</span>
                </h1>
                <p>
                    We're not just a software company – we're your partner in transforming business operations. 
                    Our mission is to make sales and inventory management more efficient, more intuitive, and more profitable.
                </p>

                <div class="trust-badge">
                    <div class="trust-icons">
                        <div class="trust-icon">🏨</div>
                        <div class="trust-icon">✓</div>
                        <div class="trust-icon">⭐</div>
                        <div class="trust-icon">💎</div>
                    </div>
                    <div class="trust-text">
                        Trusted by <strong>500+</strong> businesses across India
                    </div>
                </div>
            </div>

            <div class="hero-image">
                <div style="color: var(--text-muted); font-size: 1rem;">Sales Management Illustration</div>
                <div class="stats-badge">
                    <h3>98% Success Rate</h3>
                    <p>In Implementation</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Journey Section -->
    <section class="journey-section">
        <h2 class="section-title" style="text-align: center; font-size: 3.5rem; margin-bottom: 2rem;">Our <span class="highlight">Journey</span></h2>
        
        <!-- Where It All Started - Left Content, Right Image -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; margin-bottom: 3rem; align-items: stretch;">
            <div class="journey-card" style="padding: 2.5rem; height: 350px; display: flex; flex-direction: column; justify-content: center;">
                <h3 style="color: #00d4ff; margin-bottom: 1.2rem; font-size: 1.6rem; border-bottom: 2px solid rgba(0, 212, 255, 0.3); padding-bottom: 0.8rem;">Where It All Started</h3>
                <p style="margin-bottom: 1.2rem; line-height: 1.8; color: rgba(255, 255, 255, 0.9); font-size: 1.05rem;">
                    Launched in 2025, Bireena Selsi was created from a simple idea — to make complex sales operations easy, transparent, and efficient.
                </p>
                <div style="background: rgba(0, 212, 255, 0.05); padding: 1.2rem; border-radius: 8px; border-left: 3px solid #00d4ff;">
                    <p style="margin: 0; line-height: 1.7; color: rgba(255, 255, 255, 0.85);">
                        Our name blends <strong style="color: #00d4ff;">"Bireena"</strong> (excellence) with <strong style="color: #ff4d94;">"Selsi"</strong> (sales), symbolizing our mission to bring excellence to every stage of the sales journey.
                    </p>
                </div>
            </div>

            <div class="journey-image" style="height: 350px;">
                <div style="color: rgba(255, 255, 255, 0.4); font-size: 1.1rem;">Journey Visualization</div>
            </div>
        </div>

        <!-- The Evolution - Left Image, Right Content -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; margin-bottom: 3rem; align-items: stretch;">
            <div class="journey-image" style="height: 350px;">
                <div style="color: rgba(255, 255, 255, 0.4); font-size: 1.1rem;">Evolution Timeline</div>
            </div>

            <div class="journey-card" style="padding: 2.5rem; height: 350px; display: flex; flex-direction: column; justify-content: center;">
                <h3 style="color: #ff4d94; margin-bottom: 1.2rem; font-size: 1.6rem; border-bottom: 2px solid rgba(255, 77, 148, 0.3); padding-bottom: 0.8rem;">The Evolution</h3>
                <p style="margin-bottom: 1rem; line-height: 1.8; color: rgba(255, 255, 255, 0.9); font-size: 1.05rem;">
                    What began as a basic lead tracking tool has evolved into a complete sales management ecosystem.
                </p>
                <div style="background: rgba(255, 77, 148, 0.05); padding: 1.2rem; border-radius: 8px; border-left: 3px solid #ff4d94;">
                    <p style="margin: 0; line-height: 1.7; color: rgba(255, 255, 255, 0.85);">
                        We listened to sales teams, managers, and business owners — adding features that solve real challenges like pipeline visibility and performance analytics.
                    </p>
                </div>
            </div>
        </div>

        <!-- Today & Beyond - Left Content, Right Image -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; align-items: stretch;">
            <div class="journey-card" style="padding: 2.5rem; height: 350px; display: flex; flex-direction: column; justify-content: center; overflow: hidden;">
                <h3 style="color: #00d4ff; margin-bottom: 1rem; font-size: 1.5rem; border-bottom: 2px solid rgba(0, 212, 255, 0.3); padding-bottom: 0.7rem;">Today & Beyond</h3>
                <p style="margin-bottom: 1rem; line-height: 1.6; color: rgba(255, 255, 255, 0.9); font-size: 1rem;">
                    Today, Bireena Selsi empowers businesses across India and beyond:
                </p>
                <div style="background: rgba(0, 212, 255, 0.05); padding: 1rem; border-radius: 8px; border-left: 3px solid #00d4ff; margin-bottom: 0.8rem;">
                    <p style="margin: 0; line-height: 1.5; color: rgba(255, 255, 255, 0.85); font-size: 0.95rem;">
                        <strong style="color: #00d4ff;">Streamline operations</strong> — from lead management to automated follow-ups<br>
                        <strong style="color: #ff4d94;">Boost productivity</strong> — helping teams close more deals
                    </p>
                </div>
                <p style="margin: 0; line-height: 1.5; color: rgba(255, 255, 255, 0.75); font-style: italic; font-size: 0.9rem;">
                    Modern automation with a human touch.
                </p>
            </div>

            <div class="journey-image" style="height: 350px;">
                <div style="color: rgba(255, 255, 255, 0.4); font-size: 1.1rem;">Future Vision</div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features-section">
        <div class="features-intro">
            <h2>Key <span class="highlight">Features</span></h2>
            <p>
                Our platform is built with powerful features that help you manage your business more efficiently, 
                improve customer satisfaction, and increase revenue.
            </p>
        </div>

        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">💰</div>
                <h3>Fast Billing System</h3>
                <p>Generate bills instantly with automated calculations, discount management, and print-ready invoices for seamless checkout experience.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">📦</div>
                <h3>Inventory Management</h3>
                <p>Track stock levels in real-time, get low-stock alerts, manage products efficiently, and never run out of your bestsellers.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">📊</div>
                <h3>Sales Analytics</h3>
                <p>Monitor daily sales, track revenue trends, analyze employee performance, and make data-driven business decisions.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">👥</div>
                <h3>Employee Management</h3>
                <p>Role-based access control, activity tracking, performance monitoring, and secure authentication for your team members.</p>
            </div>
        </div>
    </section>

    <!-- Team Section -->
    <section class="team-section">
        <div class="team-intro">
            <h2>Meet Our <span class="highlight">Team</span></h2>
            <p>
                The passionate individuals behind Bireena Salesy who are dedicated to revolutionizing sales management.
            </p>
        </div>

        <div class="team-grid">
            <div class="team-card">
                <div class="team-image">
                    <img src="/images/img1.jpeg" alt="Deepika Singh" loading="lazy" onerror="this.style.display='none'; this.parentElement.innerHTML='Image Not Available';">
                </div>
                <div class="team-info">
                    <h3>Deepika Singh</h3>
                    <p class="role">Founder & Co</p>
                    <p>Leading the company with vision and innovation towards excellence.</p>
                    <div class="team-social">
                        <a href="#" class="social-icon" target="_blank" title="LinkedIn">
                            <i class="bi bi-linkedin"></i>
                        </a>
                        <a href="https://www.instagram.com/bireenainfo?igsh=MXMxN2l1a25heGFheg==" class="social-icon" target="_blank" title="Instagram">
                            <i class="bi bi-instagram"></i>
                        </a>
                    </div>
                </div>
            </div>

            <div class="team-card">
                <div class="team-image">
                    <img src="/images/img2.jpeg" alt="Ankit Kumar Gupta" loading="lazy" onerror="this.style.display='none'; this.parentElement.innerHTML='Image Not Available';">
                </div>
                <div class="team-info">
                    <h3>Ankit Kumar Gupta</h3>
                    <p class="role">Full Stack Developer</p>
                    <p>Building robust applications with 1 year of experience in modern web technologies.</p>
                    <div class="team-social">
                        <a href="https://www.linkedin.com/in/ankit-kumar-gupta-0584b8250" class="social-icon" target="_blank" title="LinkedIn">
                            <i class="bi bi-linkedin"></i>
                        </a>
                        <a href="https://www.instagram.com/ankit_roy2005?igsh=MXJrb2wxMzF6czl1cA==" class="social-icon" target="_blank" title="Instagram">
                            <i class="bi bi-instagram"></i>
                        </a>
                    </div>
                </div>
            </div>

            <div class="team-card">
                <div class="team-image">
                    <img src="/images/img3.jpeg" alt="Nirdesh Kumar" loading="lazy" onerror="this.style.display='none'; this.parentElement.innerHTML='Image Not Available';">
                </div>
                <div class="team-info">
                    <h3>Nirdesh Kumar</h3>
                    <p class="role">Full Stack Developer</p>
                    <p>Creating innovative solutions with 1 year of experience in full stack development.</p>
                    <div class="team-social">
                        <a href="https://www.linkedin.com/in/nirdesh-kumar-84b560295" class="social-icon" target="_blank" title="LinkedIn">
                            <i class="bi bi-linkedin"></i>
                        </a>
                        <a href="#" class="social-icon" target="_blank" title="Instagram">
                            <i class="bi bi-instagram"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-brand">
                <h3>BIREENA<span class="highlight">SALESY</span></h3>
                <div class="footer-contact">
                    <div class="contact-item">
                        <svg class="contact-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                            <circle cx="12" cy="10" r="3"></circle>
                        </svg>
                        <span>B-36, Anisabad, Patna, Bihar, India<br>8000025</span>
                    </div>
                    <div class="contact-item">
                        <svg class="contact-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
                        </svg>
                        <span>+91 91351-55931 , +91 93049-42225</span>
                    </div>
                    <div class="contact-item">
                        <svg class="contact-icon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                            <polyline points="22,6 12,13 2,6"></polyline>
                        </svg>
                        <span>bireenainfo@gmail.com</span>
                    </div>
                </div>
                <div class="social-links">
                    <a href="https://www.instagram.com/bireenainfo/" target="_blank" rel="noreferrer" class="social-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                            <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                            <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                        </svg>
                    </a>
                    <a href="https://www.facebook.com/people/Bireena-Bireena/61572904348705/" target="_blank" rel="noreferrer" class="social-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path>
                        </svg>
                    </a>
                    <a href="https://www.youtube.com/@Bireenainfotech" target="_blank" rel="noreferrer" class="social-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"></path>
                            <polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"></polygon>
                        </svg>
                    </a>
                </div>
            </div>

            <div class="footer-section">
                <div class="footer-card">
                    <h4>Services</h4>
                    <ul class="footer-links">
                        <li><a href="/about">About Us</a></li>
                        <li><a href="/features">Features</a></li>
                        <li><a href="#">Pricing</a></li>
                        <li><a href="/contact">Contact</a></li>
                    </ul>
                </div>
            </div>

            <div class="footer-section">
                <div class="footer-card">
                    <h4>Support</h4>
                    <ul class="footer-links">
                        <li><a href="/contact">Contact</a></li>
                        <li><a href="#">FAQ</a></li>
                        <li><a href="#">Privacy Policy</a></li>
                        <li><a href="#">Terms of Service</a></li>
                    </ul>
                </div>
            </div>

            <div class="footer-section">
                <div class="footer-card">
                    <h4>Opening Hours</h4>
                    <div class="footer-hours">
                        <div style="padding-bottom: 1rem; border-bottom: 1px solid rgba(124, 58, 237, 0.15);">
                            <p>Monday - Friday</p>
                            <p class="time">11:00 AM - 10:00 PM</p>
                        </div>
                        <div style="padding-top: 1rem;">
                            <p>Saturday - Sunday</p>
                            <p class="time">12:00 PM - 11:00 PM</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="footer-bottom">
            <p>© 2025 <strong>Bireena Salesy</strong>. All rights reserved. Designed & Developed by <a href="#">Bireena Infotech</a></p>
        </div>
    </footer>

    <!-- Scroll to Top Button -->
    <button id="scrollToTop" class="scroll-to-top" title="Go to top">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M12 19V5M12 5L5 12M12 5L19 12" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
    </button>

    <script>
        // Mobile Menu Toggle Function
        document.addEventListener('DOMContentLoaded', function() {
            const hamburgerBtn = document.getElementById('hamburgerBtn');
            const navMenu = document.getElementById('navMenu');
            const navOverlay = document.getElementById('navOverlay');
            
            // Check if all required elements exist
            if (!hamburgerBtn || !navMenu || !navOverlay) {
                console.error('Mobile menu elements not found');
                return;
            }
            
            const navLinks = navMenu.querySelectorAll('a');
            
            // Toggle menu
            function toggleMenu() {
                const isActive = navMenu.classList.toggle('active');
                navOverlay.classList.toggle('active');
                hamburgerBtn.classList.toggle('active');
                hamburgerBtn.setAttribute('aria-expanded', isActive);
                
                // Prevent body scroll when menu is open
                document.body.style.overflow = isActive ? 'hidden' : '';
            }
            
            // Close menu
            function closeMenu() {
                navMenu.classList.remove('active');
                navOverlay.classList.remove('active');
                hamburgerBtn.classList.remove('active');
                hamburgerBtn.setAttribute('aria-expanded', 'false');
                document.body.style.overflow = '';
            }
            
            // Event listeners
            hamburgerBtn.addEventListener('click', toggleMenu);
            navOverlay.addEventListener('click', closeMenu);
            
            // Close menu when clicking nav links
            navLinks.forEach(link => {
                link.addEventListener('click', () => {
                    if (window.innerWidth <= 1024) {
                        closeMenu();
                    }
                });
            });
            
            // Close menu on escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && navMenu.classList.contains('active')) {
                    closeMenu();
                    hamburgerBtn.focus();
                }
            });
            
            // Handle window resize
            let resizeTimer;
            window.addEventListener('resize', () => {
                clearTimeout(resizeTimer);
                resizeTimer = setTimeout(() => {
                    if (window.innerWidth > 1024) {
                        closeMenu();
                    }
                }, 250);
            });
        });

        // Scroll to Top Button Functionality
        const scrollToTopBtn = document.getElementById('scrollToTop');
        
        // Show/hide button based on scroll position
        window.addEventListener('scroll', () => {
            if (window.pageYOffset > 300) {
                scrollToTopBtn.classList.add('show');
            } else {
                scrollToTopBtn.classList.remove('show');
            }
        });
        
        // Smooth scroll to top when clicked
        scrollToTopBtn.addEventListener('click', () => {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });
    </script>
</body>
</html>
